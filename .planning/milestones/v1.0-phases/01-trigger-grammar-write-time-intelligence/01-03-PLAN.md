---
phase: 01-trigger-grammar-write-time-intelligence
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - lib/memory_surface.py
  - tests/memory_surface/test_dedup_placement.py
autonomous: true
requirements: [CORE-07, ORG-04]
must_haves:
  truths:
    - "A NEW-file box-store memory write whose similarity to an existing memory is at or above the backstop threshold is denied, and the deny reason names the existing file's path (consolidation target)"
    - "Writing the same content TO the named existing file (consolidation) is allowed — the backstop only blocks new-file duplicates"
    - "A memory whose grammar-known tags all carry placement box, targeted OUTSIDE the box store, is denied with the correct absolute box-store path in the reason"
    - "A memory with no grammar-known tags (ambiguous subject) targeted outside the box store is allowed — fail open (D-15)"
    - "write-context emits one composite (schema + grammar vocabulary or digest + top-5 dedup candidates + placement guidance) never exceeding 9,500 chars, and exits 0 emitting nothing on infra faults"
  artifacts:
    - path: "lib/memory_surface.py"
      provides: "dedup_candidates(), dedup backstop + placement gate in check_write, write_context() + CLI subcommand"
      contains: "def dedup_candidates"
    - path: "tests/memory_surface/test_dedup_placement.py"
      provides: "Spec-first contract tests for D-11/D-12/D-13/D-15/D-08"
      min_lines: 120
  key_links:
    - from: "lib/memory_surface.py write_context()"
      to: "_load_catalog"
      via: "dedup candidate lookup over existing catalog"
      pattern: "_load_catalog"
    - from: "lib/memory_surface.py check_write()"
      to: "parse_grammar_md placement hints"
      via: "placement gate consults grammar entries for the memory's tags"
      pattern: "placement"
    - from: "lib/memory_surface.py main()"
      to: "write_context"
      via: "write-context CLI subcommand reading event JSON on stdin"
      pattern: "write-context"
---

<objective>
Give the engine the dedup and placement intelligence (CORE-07, ORG-04): two-layer dedup (advisory candidates + fail-closed new-file backstop, D-11/D-12), the graduated store-placement gate that structurally kills the dark-memory mis-placement class (D-13/D-15), and the `write-context` subcommand that builds the entire budget-allocated composite the context hook will inject (D-08).

Purpose: dedup-before-derivation is satisfied by mechanism design — candidates arrive in the SAME injection as the trigger schema, so consolidate-vs-new is decided before triggers are authored (D-12). The placement gate makes the CLAUDE.md.fragment route-by-SUBJECT policy machine-checkable via the grammar's per-tag placement hints.
Output: `dedup_candidates()`, extended `check_write()` (backstop + placement), `write_context()` + CLI arm, spec-first contract tests. Hooks still untouched (D-18 — plan 01-04 deploys).
</objective>

## Phase Goal

**As a** Claude session writing a memory on this box, **I want to** save it with triggers derived in-context under one unified grammar, deduplicated against the store, and placed in the correct store by subject, **so that** the right memory surfaces at the right moment with zero human curation.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-CONTEXT.md
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-PATTERNS.md
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-02-SUMMARY.md
</context>

<interfaces>
<!-- Verified current engine contracts. Build against these exactly. -->

- `_load_catalog(memdir) -> dict | None` — returns parsed `_memory_catalog.json` or None on missing/corrupt (fail closed for search; for dedup treat None as "no candidates"). Catalog entries carry: id, file, path, name, description (220-char cap), type, tags, canonicalTags, lastReviewed, declineCount.
- `collections` is NOT yet imported in the engine — `Counter` must be imported at module top (stdlib only; the single new import this phase allows).
- From plan 01-01: `parse_grammar_md(path)` → entries with `placement` per tag; `PLACEMENTS`.
- From plan 01-02: `check_write(memdir, content, target=None)`, `_classify_target(target, memdir)` ("box" | "other"), `TRIGGER_SCHEMA_HINT`, `TRIGGER_FIELDS`.
- CLI conventions: `search` reads event JSON from stdin or `--event FILE`; mirror that for `write-context`. Event shape (PreToolUse): `{"tool_name": "Write", "tool_input": {"file_path": "...", "content": "..."}, "cwd": "..."}`.
- Store path derivation in Python: `resolve_memdir()` (honors MEMORY_SURFACE_DIR). The correct-store path for deny reasons/guidance comes from this, never hardcoded.
- pytest 9.0.3 is installed — selective class runs use `pytest <file> -k <expr>`.
</interfaces>

## Artifacts this phase produces (this plan's share)

- Function `dedup_candidates(memdir, proposed_tags, proposed_desc, top_n=5)` in `lib/memory_surface.py`
- Constant `DEDUP_BACKSTOP_THRESHOLD = 0.85` in `lib/memory_surface.py`
- Extended `_classify_target(target, memdir)` returning "box" | "project-store" | "repo-memory" | "other"
- Placement gate + dedup backstop branches inside `check_write()`
- Function `write_context(memdir, event)` in `lib/memory_surface.py` (returns composite text, possibly empty)
- Constant `WRITE_CONTEXT_BUDGET = 9500` in `lib/memory_surface.py`
- Function `_grammar_digest(entries)` in `lib/memory_surface.py` (one line per tag: `tag: gloss [placement]`)
- CLI subcommand `write-context` in `lib/memory_surface.py main()`
- Import `from collections import Counter` in `lib/memory_surface.py`
- File `tests/memory_surface/test_dedup_placement.py` (test classes DedupCandidates, DedupBackstop, PlacementGate, WriteContextComposite)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spec-first contract tests for dedup, placement, and write-context (RED) (D-08, D-11, D-12, D-13, D-15, D-19)</name>
  <files>tests/memory_surface/test_dedup_placement.py</files>
  <read_first>
    - .planning/phases/01-trigger-grammar-write-time-intelligence/01-CONTEXT.md — decisions D-08, D-11, D-12, D-13, D-15 (the spec these tests pin)
    - .planning/phases/01-trigger-grammar-write-time-intelligence/01-RESEARCH.md — sections "Pattern 3: Write Hook Budget Allocation" and "Pattern 4: Dedup Similarity Scoring" (score formula + budget table)
    - tests/memory_surface/test_write_triggers.py (fixture conventions from plan 01-02 — reuse the store builder)
  </read_first>
  <behavior>
    - dedup_candidates (D-11 L1): returns top-N (score, mem) sorted descending; score = 0.6 * tag_overlap + 0.4 * bag-of-words cosine on descriptions; a synthetic genuinely-duplicate pair (same tags, near-identical description) scores >= 0.85; a similar-looking-but-distinct pair (shared domain vocabulary, different tags/subject) scores < 0.85 — the threshold must separate them (pin the value 0.85; adjust fixtures, not the threshold)
    - Backstop deny (D-11 L2): check_write with target = a NEW file path in the box store and content near-duplicate of a catalog entry → rc 2, reason contains the existing memory's path and the word "consolidate"
    - Consolidation allowed (D-11): same content with target = the EXISTING memory's own path → rc 0 (overwrite/consolidation is the intended resolution)
    - Backstop skipped without target: check_write with target=None never runs the dedup backstop (cannot distinguish new-vs-overwrite)
    - Placement deny (D-15): fixture grammar where tags x,y carry placement box; memory tagged [x] with target /tmp/somerepo/memory/foo.md → rc 2, reason contains the absolute box-store path (str of the fixture memdir) — the self-healing correct destination
    - Placement fail-open (D-15): memory whose tags are unknown to the grammar, same non-box target → rc 0; memory with mixed placements (one box, one either) → rc 0; non-box targets skip tag validation and the triggers requirement entirely (those stores have no grammar — ambiguous, allow)
    - write_context (D-08): for a box-store Write event, output contains (a) the TRIGGER_SCHEMA_HINT text, (b) grammar vocabulary (full _grammar.md text when it fits), (c) up to 5 dedup candidate lines each carrying id + description + path, (d) placement guidance naming the box-store path; total length <= 9500
    - Digest fallback (D-08): with an oversized fixture grammar (pad entries so the full artifact would blow the budget), output uses the one-line-per-tag digest form `tag: gloss [placement]` and stays <= 9500
    - Fail open (D-08): missing catalog → composite still emitted without candidates; missing grammar → composite still emitted with schema + placement; non-memory event (no file_path) → empty string; write_context never raises
  </behavior>
  <action>
    Create `tests/memory_surface/test_dedup_placement.py` — stdlib unittest, TempStore pattern, MEMORY_SURFACE_DIR-isolated fixture stores with: _tags.md (small active set), _grammar.md (tags with placement hints), 2-3 memory files plus a real catalog (call ms.rebuild(store) in fixture setup so _memory_catalog.json exists). Name the four test classes EXACTLY DedupCandidates, DedupBackstop, PlacementGate, WriteContextComposite — Task 2's selective verification depends on these names. Each test method docstring cites its decision ID. Cover every behavior in the behavior block. Run the file: it MUST fail RED (dedup_candidates/write_context do not exist). Do NOT touch lib/memory_surface.py in this task.
  </action>
  <verify>
    <automated>test -f tests/memory_surface/test_dedup_placement.py && { python3 tests/memory_surface/test_dedup_placement.py 2>&1 | grep -qE "(FAILED|Error)" ; }</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tests/memory_surface/test_dedup_placement.py` FAILS (RED) before implementation
    - Test classes named DedupCandidates, DedupBackstop, PlacementGate, WriteContextComposite exist (`grep -c '^class ' tests/memory_surface/test_dedup_placement.py` >= 4)
    - The duplicate-vs-distinct synthetic pair test pins the 0.85 threshold by asserting one side denies and the other allows
    - Placement tests assert the deny reason contains the fixture store's absolute path string
    - Budget tests assert len(output) <= 9500 in both full and digest modes
    - `git diff --name-only` shows only the new test file
  </acceptance_criteria>
  <done>Contract tests for dedup, placement, and the write-context composite exist and fail RED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dedup_candidates + backstop + placement gate (GREEN except WriteContextComposite) (D-11, D-13, D-15)</name>
  <files>lib/memory_surface.py</files>
  <read_first>
    - lib/memory_surface.py (current state — check_write with _classify_target from plan 01-02, _load_catalog, parse_grammar_md)
    - tests/memory_surface/test_dedup_placement.py (the contract — Task 1)
  </read_first>
  <behavior>
    - DedupCandidates, DedupBackstop, PlacementGate classes pass; WriteContextComposite stays RED (Task 3)
    - Full pre-existing suite stays green
  </behavior>
  <action>
    Extend `lib/memory_surface.py`:
    - Add `from collections import Counter` to the import block.
    - `dedup_candidates(memdir, proposed_tags, proposed_desc, top_n=5)`: load catalog via _load_catalog (None → []); per memory compute tag_overlap = |proposed ∩ mem.tags| / max(len(proposed_tags), 1) and bag-of-words cosine over lowercased whitespace-split descriptions using Counter (intersection-sum over product of L2 norms; zero denominator → 0.0); score = 0.6 * tag_overlap + 0.4 * cosine; return top_n (score, mem) pairs sorted descending. Stdlib only — RESEARCH.md Pattern 4 verbatim.
    - `DEDUP_BACKSTOP_THRESHOLD = 0.85` module constant (conservative — blocks only near-certain duplicates; pinned by the contract tests; adjust fixtures, never loosen silently).
    - Extend `_classify_target(target, memdir)`: "box" (None or under str(memdir)); "project-store" (path contains a `/.claude/projects/` segment followed later by `/memory/`); "repo-memory" (has a `/memory/` path component with a non-infra .md basename); else "other". Pure string/Path logic on the already-canonicalized target.
    - `check_write` box branch, AFTER tag and trigger validation: when target is a box path AND `not Path(target).exists()` (new file), run dedup_candidates with the proposed tags + description; if best score >= DEDUP_BACKSTOP_THRESHOLD → rc 2, reason: "memory appears to duplicate <id>; consolidate into <existing absolute path> instead of creating a new file." Target absent (None) → skip the backstop (D-11 is new-file only).
    - `check_write` non-box branch ("project-store"/"repo-memory"): SKIP legacy tag validation and the triggers requirement (no grammar authority over foreign stores — D-15 ambiguity rule); run ONLY the placement gate: load parse_grammar_md(memdir / "_grammar.md"); collect the memory's tags that exist in the grammar; if that set is non-empty AND every one carries placement == "box" → rc 2, reason: "this memory's tags (<tags>) are box-placement; write it to <str(resolve_memdir()-derived store)>/<basename> instead." This is the observed dark-memory class (a box-subject memory written into a lab memory/ dir) denying with the correct destination. Any other combination (unknown tags, mixed placement, project/either hints) → rc 0. The gate is one-directional in Phase 1: project-subject memories written INTO the box store stay advisory-only (write-context guidance), per D-15's deny-only-high-confidence rule.
    - "other" targets: unchanged pass-through.
  </action>
  <verify>
    <automated>pytest tests/memory_surface/test_dedup_placement.py -q -k "not WriteContextComposite" && pytest tests/memory_surface/ -q -k "not WriteContextComposite" && python3 -c "import sys; sys.path.insert(0,'lib'); import memory_surface as ms; assert ms.DEDUP_BACKSTOP_THRESHOLD == 0.85; assert callable(ms.dedup_candidates)"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/memory_surface/test_dedup_placement.py -q -k "not WriteContextComposite"` exits 0 (dedup + placement GREEN)
    - `pytest tests/memory_surface/ -q -k "not WriteContextComposite"` exits 0 (entire pre-existing suite still green)
    - Import probe confirms DEDUP_BACKSTOP_THRESHOLD == 0.85 and dedup_candidates is callable
    - The dark-memory fixture deny reason contains the fixture box-store absolute path (self-healing destination)
    - No edits to search(), extract_tokens(), score_memory(), rebuild() bodies (read path untouched)
  </acceptance_criteria>
  <done>Backstop dedup and the graduated placement gate enforce CORE-07/ORG-04 at the engine layer, pinned by spec-first tests</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement write_context composite builder + CLI (GREEN, full suite) (D-08, D-12, D-13)</name>
  <files>lib/memory_surface.py</files>
  <read_first>
    - lib/memory_surface.py (current state including Task 2 additions — dedup_candidates, _classify_target)
    - tests/memory_surface/test_dedup_placement.py (WriteContextComposite contract)
    - hooks/memory-write-context.sh (current injection preamble style — the composite replaces its MSG body; do NOT edit the hook in this plan)
  </read_first>
  <behavior>
    - All test_dedup_placement.py classes pass (GREEN), full suite green
  </behavior>
  <action>
    Implement the composite builder — the single engine call plan 01-04's hook will make:
    - `_grammar_digest(entries) -> str`: one line per tag, format `tag: gloss [placement]`, sorted by tag name.
    - `WRITE_CONTEXT_BUDGET = 9500` module constant (500-char headroom under the 10,000-char additionalContext cap).
    - `write_context(memdir, event) -> str`: extract file_path/content/cwd from the event (absent file_path or non-.md basename or infra basename `_*`/`MEMORY.md` → return ""). Parse the proposed content's frontmatter for tags + description (malformed → proceed with empty values; never raise). Build the composite in this order, tracking length:
      1. Fixed preamble (~200 chars): this is a memory write; a metadata.triggers block derived from the work just done is REQUIRED at save time (CORE-02); dedup candidates and placement guidance follow.
      2. TRIGGER_SCHEMA_HINT + one worked example (reuse the plan 01-02 constant — D-08 component a).
      3. Grammar vocabulary (D-08 component b): full `(memdir / "_grammar.md").read_text()` if the running total stays within budget, else `_grammar_digest(parse_grammar_md(...))`. Missing grammar file → omit the section.
      4. Dedup candidates (D-08 component c, D-12): when the target classifies as "box", top-5 from dedup_candidates rendered as `- <id> — <description> (<path>)` lines under a header instructing: "If this memory overlaps one of these, WRITE INTO that existing file (consolidate) instead of creating a new one." Missing catalog or non-box target → omit the section.
      5. Placement guidance (D-08 component d, D-13): the route-by-SUBJECT policy sentence (box-general facts → box-brain store; lab/project-specific → that project's store — mirrors CLAUDE.md.fragment) plus the runtime-derived box-store absolute path; when the target is non-box and the proposed tags carry box placement hints, state explicitly that this memory belongs at the box-store path.
      Enforce len(result) <= WRITE_CONTEXT_BUDGET: if the full grammar pushed it over, rebuild with the digest; if still over, truncate the candidate list, then the digest tail. Any unexpected exception → return "" (a context hook may never block — fail open).
    - CLI: add a `write-context` arm to main() mirroring `search` (event JSON from stdin or `--event FILE`; print the composite to stdout; ALWAYS return 0, even for empty output).
    - Run the full suite plus a live smoke: feed a realistic box-store Write event (RESEARCH.md "Sample JSON Stdin" shape) through `python3 lib/memory_surface.py write-context` against the live store and confirm a non-empty composite under budget.
  </action>
  <verify>
    <automated>python3 tests/memory_surface/test_dedup_placement.py && pytest tests/memory_surface/ -q && python3 lib/memory_surface.py write-context < /dev/null; test $? -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tests/memory_surface/test_dedup_placement.py` exits 0 (all four classes GREEN)
    - `pytest tests/memory_surface/ -q` exits 0 (full suite)
    - Live smoke: a box-store Write event piped to `python3 lib/memory_surface.py write-context` prints a composite that is non-empty, < 9500 bytes, contains "triggers:" and the box-store absolute path, exit 0
    - The same command with a non-memory file_path prints nothing, exit 0
    - `python3 lib/memory_surface.py write-context < /dev/null` exits 0 (empty/malformed event fails open)
  </acceptance_criteria>
  <done>One engine call produces the complete budget-allocated write-time composite; hooks remain untouched and ready for plan 01-04</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Event JSON (tool_input content/file_path) → write_context | Untrusted proposed memory content parsed for tags/description |
| Catalog + grammar files → composite output | Store-resident files folded into text later injected into model context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Tampering | placement gate path classification | mitigate | Operates on the hook's realpath -sm lexically-canonicalized target; string-prefix compare against resolve_memdir(); `..` segments already collapsed so traversal cannot fake an in-store or out-of-store target |
| T-01-08 | Spoofing | composite injection content | mitigate | write_context emits plain text; the hook wraps it with `jq -cn --arg` (correct JSON escaping); grammar/catalog are user-owned local files already gated by the write guard — no network or third-party content enters the composite |
| T-01-09 | Information Disclosure | dedup deny reason names existing memory paths | accept | Paths are local, user-owned, and already surfaced by the recall hook; naming them is the intended self-healing behavior |
| T-01-10 | Denial of Service | backstop false-positive blocking legitimate memories | mitigate | Threshold 0.85 conservative + pinned by a distinct-pair contract test; deny names the consolidation path so the retry resolves; Edit path remains fail-open |
| T-01-SC | Tampering | package installs | accept | No package installs this phase (stdlib only — audit table vacuous) |
</threat_model>

<verification>
- `pytest tests/memory_surface/ -q` green
- Live smoke of write-context (non-empty, under budget, fail-open on garbage input)
- `git diff --name-only` touches only lib/memory_surface.py and the new test file (no hooks)
</verification>

<success_criteria>
- CORE-07 satisfied at engine layer: advisory candidates computed + fail-closed new-file backstop, dedup decided in the same injection as trigger derivation (D-12)
- ORG-04 satisfied at engine layer: dark-memory class denies with the correct destination; ambiguity fails open (D-15)
- D-08 composite proven under the 10k cap with automatic digest fallback
</success_criteria>

<output>
Create `.planning/phases/01-trigger-grammar-write-time-intelligence/01-03-SUMMARY.md` when done. Record: the write-context CLI contract (input shape, output, rc), final composite section order and measured live size, the _classify_target final semantics, and the threshold values pinned by tests.
</output>
