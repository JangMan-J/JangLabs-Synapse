---
phase: 01-trigger-grammar-write-time-intelligence
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - lib/memory_surface.py
  - tests/memory_surface/test_write_triggers.py
  - tests/memory_surface/test_phase1.py
  - tests/memory_surface/test_phase2.py
  - tests/memory_surface/test_phase3.py
autonomous: true
requirements: [CORE-02]
must_haves:
  truths:
    - "A full Write of a box-store memory whose frontmatter lacks a metadata.triggers block is denied (rc 2) and the deny reason carries the minimal triggers schema (self-healing retry)"
    - "A triggers block whose only behavioral evidence is generic verbs (or a single overly-broad glob) is denied by the specificity gate"
    - "parse_frontmatter reads a nested metadata.triggers block into a dict of string lists, and generate_frontmatter re-emits it losslessly (round-trip)"
    - "rebuild() still catalogs legacy memories that have no triggers block — existing memories are never retroactively invalidated"
    - "triggers field names are exactly commands/paths/args/synonyms — identical to the grammar's evidence fields (one grammar, one matcher)"
  artifacts:
    - path: "lib/memory_surface.py"
      provides: "triggers frontmatter parse/emit, _check_triggers shape+specificity validation, check-write --target plumbing"
      contains: "def _check_triggers"
    - path: "tests/memory_surface/test_write_triggers.py"
      provides: "Spec-first contract tests for D-07/D-09/D-10/D-04"
      min_lines: 100
  key_links:
    - from: "lib/memory_surface.py check_write()"
      to: "_check_triggers"
      via: "called for box-store memory writes after tag validation"
      pattern: "_check_triggers"
    - from: "lib/memory_surface.py parse_frontmatter()"
      to: "meta['triggers']"
      via: "nested-block reader under metadata:"
      pattern: "triggers"
    - from: "lib/memory_surface.py main() check-write arm"
      to: "check_write(memdir, content, target=...)"
      via: "--target value flag"
      pattern: "--target"
---

<objective>
Make the engine understand and enforce write-time triggers (CORE-02, D-07, D-09, D-10): per-memory `triggers:` lives nested under `metadata:` in frontmatter using the SAME field vocabulary as the grammar (D-04), full Writes of box-store memories without valid triggers are denied with a deny-teaches-schema reason, and a specificity gate rejects generic-only trigger sets.

Purpose: triggers must be embedded at save time while the authoring model is in-context — the engine is the validation backstop that makes that non-optional. Engine lands tested BEFORE any hook edit (D-18: hooks are live via symlink; plan 01-04 deploys).
Output: extended `parse_frontmatter`/`generate_frontmatter`, `_check_triggers()`, extended `check_write()` with optional `--target`, spec-first contract tests.
</objective>

## Phase Goal

**As a** Claude session writing a memory on this box, **I want to** save it with triggers derived in-context under one unified grammar, deduplicated against the store, and placed in the correct store by subject, **so that** the right memory surfaces at the right moment with zero human curation.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-CONTEXT.md
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-PATTERNS.md
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Verified current engine contracts (lib/memory_surface.py). Build against these exactly. -->

- `parse_frontmatter(text) -> (top, meta, body)` — indented lines under `metadata:` are flat children split on ":"; `tags:` has a special flow + block-list reader. WITHOUT extension, a nested `triggers:` block leaks its sub-keys (commands/paths/...) into meta as flat string keys — the nested-block reader you add must consume those lines itself (peek-forward loop, same style as the block-list tags reader at the `k == "tags"` branch).
- `generate_frontmatter(top, meta, body)` — `META_ORDER = ["node_type", "type", "tags", "originSessionId", "lastReviewed", "declineCount", "nextEligible"]` drives emission; `emit()` special-cases `tags` as a flow list. Add `"triggers"` to META_ORDER (immediately after `"tags"`) and special-case it as a nested block.
- `check_write(memdir, content) -> (rc, reason)` — rc 2 + non-empty reason is the deny contract; rejects top-level `tags:`; validates each tag against parse_tags_md(memdir/"_tags.md") active set. Tag validation stays on legacy `_tags.md` in Phase 1 (cutover to grammar is Phase 2 — do NOT migrate it).
- `GENERIC_VERBS` — module-level set (restart/start/stop/status/.../update/upgrade). Reuse as-is for the specificity gate.
- CLI: `main()` check-write arm reads stdin (or `--content-file`), prints deny reason on STDOUT, returns rc. `VALUE_FLAGS` set must gain `"--target"`. The live guard pipes content with NO --target — that default must behave as a box-store memory write (back-compat for the live hook until plan 01-04 updates it).
- From plan 01-01: `parse_grammar_md`, `validate_grammar`, `GRAMMAR_FIELDS` (field-name source of truth for D-04 uniformity).
</interfaces>

## Artifacts this phase produces (this plan's share)

- Function `_check_triggers(triggers)` in `lib/memory_surface.py` (shape + specificity validation, returns (rc, reason))
- Constant `TRIGGER_SCHEMA_HINT` in `lib/memory_surface.py` (the minimal schema embedded in every triggers deny reason)
- Constant `TRIGGER_FIELDS = ("commands", "paths", "args", "synonyms")` in `lib/memory_surface.py`
- Constant `BROAD_GLOBS = {"*", "**", "/**", "~/**"}` in `lib/memory_surface.py`
- Helper `_classify_target(target, memdir)` in `lib/memory_surface.py`
- Extended signature `check_write(memdir, content, target=None)` in `lib/memory_surface.py`
- META_ORDER entry `"triggers"` + nested-block emission in `generate_frontmatter`
- Nested `triggers:` reader in `parse_frontmatter` (meta["triggers"] becomes dict of string lists)
- CLI flag `--target` on the `check-write` subcommand (added to VALUE_FLAGS)
- File `tests/memory_surface/test_write_triggers.py`

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spec-first contract tests for triggers parse/validate (RED) (D-04, D-07, D-09, D-10, D-19)</name>
  <files>tests/memory_surface/test_write_triggers.py</files>
  <read_first>
    - .planning/phases/01-trigger-grammar-write-time-intelligence/01-CONTEXT.md — decisions D-04, D-07, D-09, D-10 (the spec these tests pin)
    - memory/_grammar.md (spec header from plan 01-01 — the canonical field names commands/paths/args/synonyms)
    - tests/memory_surface/test_grammar.py (fixture/store conventions established in plan 01-01)
    - lib/memory_surface.py — parse_frontmatter, generate_frontmatter, check_write, GENERIC_VERBS (current signatures only; do NOT assert implementation internals)
  </read_first>
  <behavior>
    - Round-trip (D-07): content with `metadata:` containing `triggers:` whose sub-lines (4-space indent) are `commands: [wpctl, pw-record]`, `paths: ["~/.config/pipewire/**"]`, `args: [set-volume]`, `synonyms: [wireplumber]` parses to meta["triggers"] == {"commands": ["wpctl", "pw-record"], "paths": ["~/.config/pipewire/**"], "args": ["set-volume"], "synonyms": ["wireplumber"]}; generate then re-parse yields the same triggers dict; sub-keys do NOT leak into meta as flat keys
    - Top-level rejection (D-07): a top-level `triggers:` key denies with a reason directing it under `metadata:` (parity with the existing top-level tags: rejection)
    - Missing triggers (D-09): check_write on box-store memory content WITHOUT a triggers block returns rc 2, reason contains the literal strings "triggers:" and "commands:" (deny-teaches-schema)
    - Shape (D-09): triggers present but commands+paths+args all empty → rc 2; an unknown trigger field name (e.g. singular "command:") → rc 2 naming the allowed fields (D-04 uniformity — field names pinned to TRIGGER_FIELDS)
    - Specificity (D-10): commands all in GENERIC_VERBS with no paths and no args → rc 2 reason mentioning generic; same generic commands plus one domain-specific arg → rc 0; paths == ["~/**"] as the ONLY behavioral evidence → rc 2 (BROAD_GLOBS); fully specific triggers → rc 0
    - Legacy preservation: rebuild() on a fixture store containing a memory with NO triggers still lists it in catalog["memories"] (never retroactively invalidated)
    - Tag validation unchanged: an unknown tag still denies exactly as before (existing check preserved alongside new checks)
    - Default target: check_write called without target applies box-store semantics (triggers required)
  </behavior>
  <action>
    Create `tests/memory_surface/test_write_triggers.py` — stdlib unittest, same structure as test_grammar.py (LAB = parents[2], sys.path.insert lib, TempStore base with tempfile store, module-level fixture strings for _tags.md / _grammar.md / memory content). Write every test FROM the D-04/D-07/D-09/D-10 decision text and the _grammar.md spec header — each test method docstring cites its decision ID (the recalled "green tests pinned to implementation" misfire is the failure mode this discipline kills). Cover every behavior in the behavior block. Use a small fixture _tags.md (2-3 valid tags) so tag validation passes and only trigger behavior is under test. Run the file: it MUST fail (RED) — _check_triggers does not exist yet and parse_frontmatter currently leaks trigger sub-keys. Do NOT touch lib/memory_surface.py in this task.
  </action>
  <verify>
    <automated>test -f tests/memory_surface/test_write_triggers.py && { python3 tests/memory_surface/test_write_triggers.py 2>&1 | grep -qE "(FAILED|Error)" ; }</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tests/memory_surface/test_write_triggers.py` FAILS (RED) before any engine change
    - Every test method docstring cites D-04, D-07, D-09 or D-10
    - Tests assert the field names commands/paths/args/synonyms literally (D-04 pin)
    - `git diff --name-only` shows only the new test file (engine untouched in this task)
  </acceptance_criteria>
  <done>Contract tests for the full triggers spec exist and fail RED against the current engine</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement triggers parse/emit + _check_triggers + --target plumbing (GREEN) (D-07, D-09, D-10)</name>
  <files>lib/memory_surface.py, tests/memory_surface/test_phase1.py, tests/memory_surface/test_phase2.py, tests/memory_surface/test_phase3.py</files>
  <read_first>
    - lib/memory_surface.py (full current state — parse_frontmatter indented-child loop, generate_frontmatter emit(), check_write, main() check-write arm, VALUE_FLAGS)
    - tests/memory_surface/test_write_triggers.py (the contract — written in Task 1)
  </read_first>
  <behavior>
    - All test_write_triggers.py tests pass (GREEN)
    - Full suite green: any legacy fixture that now violates D-09 (full-Write memory content without triggers asserted rc 0) is fixed by ADDING a valid triggers block to the fixture — never by weakening a new check
  </behavior>
  <action>
    Extend `lib/memory_surface.py` (no new imports needed):
    - `parse_frontmatter`: in the indented-child branch, when `k == "triggers"` and v is empty, consume following lines with DEEPER indentation, parsing `commands/paths/args/synonyms: [...]` values via the _parse_flow_tags strip-brackets/split/strip-quotes approach into a dict; set meta["triggers"] to that dict and resume the outer loop at the first non-deeper line (mirror the block-list tags reader's peek-forward style). Unknown sub-keys are kept in the dict — validation rejects them later; parsing never crashes.
    - `generate_frontmatter`: add "triggers" to META_ORDER after "tags"; in emit(), when k == "triggers" and the value is a dict, emit `  triggers:` then one `    <field>: [a, b]` line per non-empty field in TRIGGER_FIELDS order. Round-trip must be lossless.
    - Constants: `TRIGGER_FIELDS = ("commands", "paths", "args", "synonyms")`; `BROAD_GLOBS = {"*", "**", "/**", "~/**"}`; `TRIGGER_SCHEMA_HINT` — a literal multi-line string showing the minimal valid block (triggers: with commands/paths/args/synonyms lines, the ≥1-behavioral rule, and one worked example) appended verbatim to every triggers deny reason (D-09 self-healing).
    - `_check_triggers(triggers) -> (rc, reason)`: not a dict → 2; field name not in TRIGGER_FIELDS → 2 naming allowed fields; any field value not a list of strings → 2; behavioral evidence empty (commands+paths+args all empty) → 2; specificity (D-10): all commands in GENERIC_VERBS AND no args AND (no paths OR every path in BROAD_GLOBS after expanding a leading ~ to the home dir) → 2 naming the generic verbs; else (0, ""). Every deny reason includes TRIGGER_SCHEMA_HINT.
    - `_classify_target(target, memdir) -> str`: returns "box" when target is None or the lexically-normalized target is under str(memdir); else "other". (Plan 01-03 extends "other" into project-store/repo-memory branches.)
    - `check_write(memdir, content, target=None)`: keep the existing top-level-tags rejection and per-tag validation against _tags.md EXACTLY as-is (legacy vocabulary remains authoritative for tags through Phase 1 — grammar cutover is Phase 2; do NOT migrate tag validation). Add: top-level `triggers:` rejection (same wording style as the tags one); then when _classify_target(...) == "box": require meta["triggers"] present and pass _check_triggers. "other" targets pass through unchanged in this plan.
    - CLI: add "--target" to VALUE_FLAGS; the check-write arm passes `_arg("--target")` through to check_write.
    - Run the FULL suite; update fixtures in test_phase1.py / test_phase2.py / test_phase3.py ONLY where they feed full memory content to check_write and assert rc 0 — add a minimal valid triggers block to those fixture strings. Do not weaken any deny assertion; do not touch hook files (D-18: hooks deploy in plan 01-04).
  </action>
  <verify>
    <automated>python3 tests/memory_surface/test_write_triggers.py && python3 -m unittest discover -s tests/memory_surface -p 'test_*.py'</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tests/memory_surface/test_write_triggers.py` exits 0 (GREEN)
    - `python3 -m unittest discover -s tests/memory_surface -p 'test_*.py'` exits 0 (full suite green, legacy fixtures upgraded not weakened)
    - Piping a memory WITHOUT triggers (valid live tag, e.g. claude-harness) into `python3 lib/memory_surface.py check-write` exits 2 and stdout contains "triggers:" (live-store smoke check of D-09 default)
    - Piping the SAME memory WITH a valid triggers block exits 0
    - `grep -n 'def check_write' lib/memory_surface.py` shows the target=None parameter; `grep -c '"--target"' lib/memory_surface.py` ≥ 1 (VALUE_FLAGS entry)
    - `git diff lib/memory_surface.py` contains no edits to search(), extract_tokens(), score_memory(), rebuild() bodies (read path untouched)
  </acceptance_criteria>
  <done>Engine enforces D-07/D-09/D-10 for box-store memory writes; round-trip lossless; full suite green; read path untouched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Write tool_input.content → check_write | Untrusted proposed file content (frontmatter) crosses into the engine via guard stdin |
| --target argv → check_write | Path string from hook; influences which checks run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | parse_frontmatter triggers reader | mitigate | Pure line/indentation scanning, no eval; malformed nesting degrades to validation deny, never an exception escaping to the guard (ASVS V5) |
| T-01-05 | Tampering | --target path classification | mitigate | Guard passes a realpath -sm lexically-canonicalized absolute path; engine compares normalized string prefixes against resolve_memdir() — a ../-escape cannot reclassify an out-of-store write as in-store |
| T-01-06 | Denial of Service | check_write fail-closed misfire | mitigate | rc 2 emitted only with non-empty reason; the guard's existing non-empty-reason gate plus its 2>/dev/null on the engine call means an interpreter error is never misclassified as a deny; .surface-disabled kill-switch remains the abort lever |
| T-01-SC | Tampering | package installs | accept | No package installs this phase (stdlib only — audit table vacuous) |
</threat_model>

<verification>
- `python3 -m unittest discover -s tests/memory_surface -p 'test_*.py'` green
- Live-store smoke: trigger-less memory content → check-write rc 2 with schema in reason; valid triggers → rc 0
- `git diff` shows no hook file changes and no read-path engine changes
</verification>

<success_criteria>
- CORE-02 engine half satisfied: triggers are validated at write time with deny-teaches-schema enforcement; field vocabulary identical to the grammar (D-04)
- Specificity gate (D-10) live and pinned by tests
- Legacy memories and the legacy read path completely unaffected
</success_criteria>

<output>
Create `.planning/phases/01-trigger-grammar-write-time-intelligence/01-02-SUMMARY.md` when done. Record: the exact TRIGGER_SCHEMA_HINT text (plan 01-03's write-context injects it), the check_write(memdir, content, target) contract and _classify_target semantics, and which legacy fixtures gained triggers blocks.
</output>
