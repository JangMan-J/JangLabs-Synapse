---
phase: 01-trigger-grammar-write-time-intelligence
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - hooks/memory-write-context.sh
  - hooks/memory-write-guard.sh
  - tests/memory_surface/test_write_hooks.sh
autonomous: true
requirements: [CORE-02, CORE-07, ORG-04]
must_haves:
  truths:
    - "A Write of a non-infra .md into a repo memory/ directory or any Claude project store is detected by BOTH write hooks (the dark-memory class is no longer invisible)"
    - "A full Write of a box-store memory without valid triggers is denied end-to-end through memory-write-guard.sh, with the schema in stderr"
    - "memory-write-context.sh injects the engine's write-context composite as PreToolUse additionalContext JSON"
    - "Kill-switch (.surface-disabled), missing jq, and missing engine all exit 0 silently in both hooks (fail open on every infra fault)"
    - "One real memory written on the live box embeds derived triggers, passed dedup/placement, and landed in the box-brain store (walking skeleton, D-20)"
    - "An attempted memory-shaped write into JangLabs/synapse/memory/ with box-placement tags is DENIED on the live box, naming the box store (dark-memory reproduction case closed)"
  artifacts:
    - path: "hooks/memory-write-context.sh"
      provides: "Widened detection + engine write-context composite injection"
      contains: "write-context"
    - path: "hooks/memory-write-guard.sh"
      provides: "Widened detection + check-write --target + _grammar.md taxonomy gating"
      contains: "--target"
    - path: "tests/memory_surface/test_write_hooks.sh"
      provides: "End-to-end sample-JSON fixture covering detect/deny/allow/fail-open matrix"
      min_lines: 60
  key_links:
    - from: "hooks/memory-write-context.sh"
      to: "lib/memory_surface.py write-context"
      via: "engine spawn with event JSON on stdin"
      pattern: "write-context"
    - from: "hooks/memory-write-guard.sh"
      to: "lib/memory_surface.py check-write --target"
      via: "content on stdin + canonicalized target path"
      pattern: "check-write --target"
    - from: "both hooks"
      to: "$STORE/.surface-disabled"
      via: "kill-switch check before any gating"
      pattern: "surface-disabled"
---

<objective>
Deploy the write-time intelligence pipeline live (CORE-02, CORE-07, ORG-04 end-to-end): both write hooks gain widened detection (D-14), the context hook injects the engine's budget-allocated composite (D-08), the guard enforces triggers/dedup/placement through `check-write --target` (D-09, D-11, D-15) — then prove the walking skeleton with one real memory write on this box (D-20).

Purpose: the hooks are LIVE via symlink — every edit takes effect on the next tool call (D-18). This plan therefore comes LAST: the engine is already tested, the e2e fixture is written and RED before any hook edit, and `.surface-disabled` is the abort lever throughout.
Output: extended hooks, end-to-end shell fixture, and a demonstrated live write pipeline (real memory with triggers in the box store + a denied dark-memory attempt).
</objective>

## Phase Goal

**As a** Claude session writing a memory on this box, **I want to** save it with triggers derived in-context under one unified grammar, deduplicated against the store, and placed in the correct store by subject, **so that** the right memory surfaces at the right moment with zero human curation.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-CONTEXT.md
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-PATTERNS.md
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Verified current hook + engine contracts. -->

- Both hooks already implement: jq presence gate, file_path/cwd extraction, $HOME-derived store key (`tr '/' '-'`), MEMORY_SURFACE_DIR override, `realpath -sm` LEXICAL canonicalization (never resolve symlinks — taxonomy files ARE symlinks), `.surface-disabled` kill-switch, infra exemptions (`_tags.md|_tag_links.md`, `MEMORY.md|_*`), `set -u`, quiet-on-success.
- Guard only: ENGINE resolution `SELF=$(readlink -f "$0")` → `$(dirname "$SELF")/../lib/memory_surface.py`, `[ -r "$ENGINE" ] || exit 0`; deny = capture engine stdout → re-emit on stderr → exit 2, gated on rc==2 AND non-empty reason; Edit/MultiEdit (no .content) fail open; taxonomy arm validates the CURRENT on-disk file via `validate` with bootstrap allowance (`[ -e "$abs" ] || exit 0`).
- Context hook only: emits `jq -cn --arg ctx "$MSG" '{hookSpecificOutput:{hookEventName:"PreToolUse",additionalContext:$ctx}}'` — the ONLY way PreToolUse injects context. The context hook currently has NO engine resolution — add the guard's readlink pattern.
- Engine (plans 01-01..03): `write-context` (event JSON stdin → composite stdout, always rc 0); `check-write --target <abs>` (content stdin or --content-file → rc 0/2, deny reason on STDOUT); `validate-grammar` (rc 0/2, errors on stderr).
- Both write hooks are registered in settings for Edit|Write|MultiEdit — widened detection needs NO settings/agent-harness change (no new hook files).
</interfaces>

## Artifacts this phase produces (this plan's share)

- Shell function `is_memory_write()` (consistent detection arms in both hooks: box store, `*/.claude/projects/*/memory/*.md`, repo `*/memory/*.md` non-infra)
- ENGINE resolution block in `hooks/memory-write-context.sh`
- `write-context` engine call + composite injection in `hooks/memory-write-context.sh`
- `check-write --target "$abs"` call in `hooks/memory-write-guard.sh`
- `_grammar.md` added to the taxonomy arm of `hooks/memory-write-guard.sh` (validated via `validate-grammar`)
- `_grammar.md` added to the context hook's infra-exemption arm
- File `tests/memory_surface/test_write_hooks.sh` (executable e2e fixture)
- One new real memory file in the box-brain store (walking-skeleton evidence, D-20)

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Spec-first end-to-end hook fixture (RED) (D-09, D-14, D-15, D-18, D-19)</name>
  <files>tests/memory_surface/test_write_hooks.sh</files>
  <read_first>
    - tests/memory_surface/test_hooks_phase1.sh (the existing shell-fixture pattern to mirror: tmp store, MEMORY_SURFACE_DIR export, printf JSON | hook, assert exit code + output)
    - hooks/memory-write-context.sh and hooks/memory-write-guard.sh (current detection/exemption order — the fixture pins the EXTENDED spec, but reuse the invocation style)
    - .planning/phases/01-trigger-grammar-write-time-intelligence/01-CONTEXT.md — D-09, D-14, D-15, D-18
    - .planning/phases/01-trigger-grammar-write-time-intelligence/01-RESEARCH.md — sections "Pattern 5: Widened Store Detection" and "Sample JSON Stdin for Offline Hook Testing"
  </read_first>
  <behavior>
    Each case = printf a PreToolUse JSON event into the hook with MEMORY_SURFACE_DIR pointed at a tmp fixture store (seeded _tags.md, _grammar.md with box-placement tags, one existing memory, rebuilt catalog). Assert exit code and stdout/stderr content:
    - GUARD deny (D-09): Write of box-store memory content WITHOUT triggers → exit 2, stderr contains "triggers:"
    - GUARD allow: same content WITH a valid metadata.triggers block → exit 0, no output
    - GUARD dark-memory deny (D-14+D-15): Write to <tmpdir>/somerepo/memory/lesson.md with box-placement tags → exit 2, stderr contains the fixture store path
    - GUARD ambiguous allow (D-15): same target, tags unknown to the grammar → exit 0
    - GUARD infra exemptions (D-14): writes targeting _grammar.md / _tags.md / MEMORY.md are never gated as memories (taxonomy arm or exempt) → exit 0 for valid states
    - GUARD Edit fail-open: Edit event (no .content) on a box memory → exit 0
    - CONTEXT inject: Write of box-store memory → exit 0, stdout is one-line JSON whose hookSpecificOutput.additionalContext contains "triggers:" (parse with jq)
    - CONTEXT widened inject (D-14): Write to a repo memory/ path (non-infra) → exit 0 with additionalContext (guidance reaches mis-placed writes too)
    - CONTEXT non-memory silence: Write to a random .md outside any store/memory dir → exit 0, empty stdout
    - Fail-open matrix (D-18, both hooks): `.surface-disabled` present in the fixture store → exit 0 silent; engine unreachable (run a copy of the hook from a tmp tree without lib/) → exit 0; malformed JSON on stdin → exit 0
  </behavior>
  <action>
    Create executable `tests/memory_surface/test_write_hooks.sh` mirroring test_hooks_phase1.sh's structure (POSIX-ish bash, `set -u`, a fail counter, tmp dirs cleaned on EXIT trap). Derive every assertion from the decision text (D-09/D-14/D-15) — NOT from the current hook code (the current hooks only watch the box store; that is the bug being fixed). Each case comment cites its decision ID. chmod +x. Run it: the dark-memory and widened-injection cases MUST FAIL (RED) against the current hooks — proof the fixture pins the spec, not the implementation. Box-store deny/allow cases may already pass (engine landed in 01-02/01-03 and the live guard already calls check-write) — that is expected; RED is required only for the widened-detection cases. Do NOT edit the hooks in this task.
  </action>
  <verify>
    <automated>test -x tests/memory_surface/test_write_hooks.sh && { ./tests/memory_surface/test_write_hooks.sh 2>&1 | grep -qiE "fail"; }</automated>
  </verify>
  <acceptance_criteria>
    - Fixture exists, is executable, and FAILS (RED) on the widened-detection cases against the current hooks
    - All asserted JSON events use the real PreToolUse shape ({"tool_name","tool_input":{"file_path","content"},"cwd"})
    - No fixture case touches the live box-brain store (every case exports MEMORY_SURFACE_DIR to a tmp store)
    - Case comments cite D-09/D-14/D-15/D-18
  </acceptance_criteria>
  <done>The end-to-end spec for the extended hooks exists as a runnable fixture, RED where the current hooks fall short</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend both write hooks — widened detection, composite injection, --target enforcement (GREEN) (D-08, D-09, D-14, D-15, D-18)</name>
  <files>hooks/memory-write-context.sh, hooks/memory-write-guard.sh</files>
  <read_first>
    - hooks/memory-write-context.sh and hooks/memory-write-guard.sh (current full source — every existing gate must survive)
    - tests/memory_surface/test_write_hooks.sh (the contract — Task 1)
    - .planning/phases/01-trigger-grammar-write-time-intelligence/01-PATTERNS.md — "Shared Patterns" section (kill-switch order, store derivation, lexical canonicalization, infra-exemption-first, jq -cn --arg)
  </read_first>
  <behavior>
    - ./tests/memory_surface/test_write_hooks.sh passes fully (GREEN)
    - pytest tests/memory_surface/ -q still green (engine untouched)
  </behavior>
  <action>
    SAFETY FIRST (D-18): hooks are live via symlink. Before editing, create the kill-switch: `touch "$HOME/.claude/projects/$(printf '%s' "$HOME" | tr '/' '-')/memory/.surface-disabled"`. Edit, test offline, THEN remove the kill-switch as the last step of this task. Never commit between an untested hook edit and its passing fixture run.

    hooks/memory-write-context.sh:
    - Keep all existing gates in order: jq check, path extraction, HOME guard, store derivation, realpath -sm canonicalization, kill-switch.
    - Add ENGINE resolution (copy the guard's readlink -f pattern); `[ -r "$ENGINE" ] || exit 0`.
    - Replace the single box-store arm (`case "$abs" in "$STORE"/*)`) with widened detection (D-14), infra exemptions FIRST: basename not *.md → exit 0; basename matching `_tags.md|_tag_links.md|_grammar.md|MEMORY.md|_*` → exit 0; then match any of: `"$STORE"/*` (box store), `*/.claude/projects/*/memory/*.md` (any project store), `*/memory/*.md` (repo memory dir — the dark-memory case). No match → exit 0.
    - Replace the _tags.md head -c injection with the engine composite: pipe the ORIGINAL hook input JSON (`printf '%s' "$input"`) into `python3 "$ENGINE" write-context 2>/dev/null`; empty output → exit 0 (fail open); else wrap with the existing `jq -cn --arg ctx` form and exit 0. The context hook never blocks (exit 0 on every path).

    hooks/memory-write-guard.sh:
    - Keep ENGINE resolution, jq gate, store derivation, canonicalization, kill-switch.
    - Same widened detection, with the taxonomy arm extended: `_tags.md|_tag_links.md` → existing `validate` flow; `_grammar.md` → same flow calling `python3 "$ENGINE" validate-grammar 2>&1` (bootstrap allowance `[ -e "$abs" ] || exit 0` kept); other `MEMORY.md|_*` → exit 0; memory-classified targets (any of the three detection arms) → TYPE=memory.
    - TYPE=memory flow: keep the Edit fail-open (`[ -n "$content" ] || exit 0`); change the engine call to `printf '%s' "$content" | python3 "$ENGINE" check-write --target "$abs" 2>/dev/null`; keep the rc==2 AND non-empty-reason deny gate verbatim (it is the interpreter-error firewall).
    - Hooks stay quiet on success, no Python spawn for non-memory paths (the shell cheap-gate still screens everything else), POSIX-ish shell + jq only.
    - Test offline: run `./tests/memory_surface/test_write_hooks.sh` until GREEN, plus one manual sample-JSON run per hook (lab convention). Then remove `.surface-disabled` and re-run the fixture once (it sets its own MEMORY_SURFACE_DIR; the live store stays untouched either way).
  </action>
  <verify>
    <automated>./tests/memory_surface/test_write_hooks.sh && pytest tests/memory_surface/ -q && bash -n hooks/memory-write-context.sh && bash -n hooks/memory-write-guard.sh && test ! -e "$HOME/.claude/projects/$(printf '%s' "$HOME" | tr '/' '-')/memory/.surface-disabled"</automated>
  </verify>
  <acceptance_criteria>
    - `./tests/memory_surface/test_write_hooks.sh` exits 0 (all cases GREEN, including dark-memory deny and widened injection)
    - `pytest tests/memory_surface/ -q` exits 0; `git diff --name-only` shows only the two hook files and the fixture (engine untouched by this task)
    - `bash -n` passes on both hooks (syntax)
    - Kill-switch removed after GREEN
    - Infra-exemption order preserved: a Write event targeting `$STORE/_grammar.md` produces NO additionalContext injection (taxonomy gating only)
  </acceptance_criteria>
  <done>Both hooks live with widened detection, composite injection, and --target enforcement; e2e fixture green; fail-open posture intact</done>
</task>

<task type="auto">
  <name>Task 3: Walking-skeleton live demonstration (D-20) — one real memory through the full pipeline</name>
  <files>~/.claude/projects/-home-jangmanj/memory/&lt;new-memory-slug&gt;.md (live store — outside the repo; key derived at runtime, never hardcoded in scripts)</files>
  <read_first>
    - .planning/phases/01-trigger-grammar-write-time-intelligence/01-CONTEXT.md — D-20 (the skeleton definition) and the <specifics> section (deny-teaches-schema expectations)
    - memory/_grammar.md (live seeded grammar — choose tags for the demo memory from it)
  </read_first>
  <action>
    Demonstrate the skeleton ON THE LIVE BOX (not via fixtures — D-20: "demonstrated by an actual memory write on the live box, not by tests alone"). The executor IS a Claude session on this box, so its own Write tool calls exercise the live hooks:
    1. POSITIVE PATH: write ONE genuine memory (a real lesson from executing this phase — a [Rewire]/[Misfire]-grade insight discovered during implementation; not a synthetic throwaway) to the box-brain store (`$HOME/.claude/projects/<key>/memory/<kebab-slug>.md`, key = $HOME with / → -). Frontmatter: name, description, metadata with node_type: memory, type, tags chosen from the live _tags.md active set that ALSO exist in _grammar.md (e.g. claude-harness), and a metadata.triggers block derived from the actual work (specific commands/paths/args — must pass the D-10 specificity gate). Expected: the context hook injects the composite; the guard allows the write — or denies with the schema, in which case fixing per the deny reason IS the mechanism working (self-heal).
    2. Verify the landed file: `python3 lib/memory_surface.py check-write --target <path> --content-file <path>` exits 0; `grep -A4 'triggers:' <path>` shows the field lines; run `python3 lib/memory_surface.py rebuild` and confirm via jq that `_memory_catalog.json` contains the new id (legacy catalog still routes it by tags — old path alive, no dark window).
    3. NEGATIVE PATH (dark-memory class closed): attempt a Write of a memory-shaped .md with box-placement tags to `/home/jangmanj/JangLabs/synapse/memory/dark-memory-probe.md`. EXPECTED OUTCOME: the tool call is DENIED by memory-write-guard with the box-store path in the reason. The deny IS the success condition — do not retry to force it through, and ensure no probe file remains in synapse/memory/.
    4. Record both outcomes (the allow evidence + the captured deny text) in the SUMMARY. If anything misbehaves on the live box: `touch $STORE/.surface-disabled`, fix offline against the fixture, re-verify, remove the switch.
  </action>
  <verify>
    <automated>STORE="$HOME/.claude/projects/$(printf '%s' "$HOME" | tr '/' '-')/memory"; NEW_MEM=$(ls -t "$STORE"/*.md | head -1); grep -q "triggers:" "$NEW_MEM" && python3 lib/memory_surface.py check-write --target "$NEW_MEM" --content-file "$NEW_MEM" && test ! -f /home/jangmanj/JangLabs/synapse/memory/dark-memory-probe.md</automated>
  </verify>
  <acceptance_criteria>
    - A new .md exists in the live box-brain store whose frontmatter contains a metadata.triggers block with at least one non-generic command/path/arg
    - `python3 lib/memory_surface.py check-write --target <that file> --content-file <that file>` exits 0
    - After rebuild, `jq -r '.memories[].id' "$STORE/_memory_catalog.json"` lists the new memory's id (legacy routing covers it — no dark window)
    - The dark-memory probe write into JangLabs/synapse/memory/ was DENIED (deny text captured in the SUMMARY) and `test ! -f /home/jangmanj/JangLabs/synapse/memory/dark-memory-probe.md` passes
    - `git -C /home/jangmanj/JangLabs/synapse status --porcelain memory/` shows no stray memory files (only the committed _grammar.md change set)
  </acceptance_criteria>
  <done>Walking skeleton proven: grammar → engine → live hooks → one real memory with derived triggers in the right store; dark-memory class demonstrably closed on the live box</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude tool_input JSON → both PreToolUse hooks | Untrusted file_path (traversal) + content (malformed frontmatter) on every Edit/Write/MultiEdit |
| Hook stderr/additionalContext → model context | Deny reasons and composite text are read by the in-context model |
| Live symlinked hooks → every session on this box | A bad edit fires immediately on the next tool call (highest-blast-radius surface this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering | path traversal vs widened detection | mitigate | realpath -sm lexical canonicalization runs BEFORE all case arms in both hooks — a `..`-laden path cannot false-classify into or out of a watched store (existing proven gate, preserved) |
| T-01-12 | Denial of Service | fail-closed misfire blocking all writes | mitigate | Deny requires rc==2 AND non-empty reason (interpreter-error firewall, kept verbatim); only memory-classified .md paths ever reach the engine; `.surface-disabled` created during development and standing as the abort lever; Edit/MultiEdit stay fail-open |
| T-01-13 | Spoofing | additionalContext JSON injection via composite content | mitigate | `jq -cn --arg` builds the JSON (escapes newlines/quotes); the composite is plain text from user-owned local files; the hook never string-concatenates content into JSON |
| T-01-14 | Elevation of Privilege | hooks writing where they should not | mitigate | Hooks write NOTHING (context emits JSON to stdout; guard emits stderr only); no permissions keys touched (standing invariant); no new settings registration needed |
| T-01-SC | Tampering | package installs | accept | No package installs this phase (stdlib + jq already present — audit table vacuous) |
</threat_model>

<verification>
- `./tests/memory_surface/test_write_hooks.sh` green (full detect/deny/allow/fail-open matrix)
- `pytest tests/memory_surface/ -q` green
- Live evidence: new memory with triggers in the box store; dark-memory probe denied; no `.surface-disabled` left behind
- Legacy read path untouched: `git diff --name-only` across the phase never lists hooks/memory-recall.sh, memory/_tags.md, memory/_tag_links.md
</verification>

<success_criteria>
- Phase success criteria 3, 4, 5 demonstrably TRUE on the live box: triggers embedded at save time in-context; dedup candidates injected before derivation; box-subject memory cannot land outside the box store
- Fail-open behaviors verified by fixture (feeds MVR items 6-7 evidence forward to Phase 2)
- The old routing path still routes everything it routed before (coexistence preserved until the Phase 2 gated cutover)
</success_criteria>

<output>
Create `.planning/phases/01-trigger-grammar-write-time-intelligence/01-04-SUMMARY.md` when done. Record: the live demo evidence (memory path + its triggers block + the captured dark-memory deny text), final hook detection arms, measured composite size on the live store, and any threshold adjustments made with their pinning tests.
</output>
