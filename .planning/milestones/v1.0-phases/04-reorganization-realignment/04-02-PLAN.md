---
phase: 04-reorganization-realignment
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - lib/memory_surface.py
  - hooks/memory-write-guard.sh
  - hooks/memory-catalog-refresh.sh
  - tests/memory_surface/test_phase1.py
  - tests/memory_surface/test_phase2.py
  - tests/memory_surface/test_write_hooks.sh
  - tests/memory_surface/test_probe_runner.py
  - tests/memory_surface/test_phase3.py
autonomous: true
requirements: [ORG-01, ORG-02]
must_haves:
  truths:
    - "The engine has no _tag_links.md write path: parse_tag_links(), link(), unlink(), synonym_map(), _drop_pair_lines(), validate()'s synonym/distinction/path-tag arm, and the link/unlink CLI dispatch arms are all gone (D-50); add-tag survives because its body provably touches only _tags.md (orchestrator resolution 2, verified mechanically before CLI surgery)"
    - "The guard and the engine moved in lockstep within this one plan: memory-write-guard.sh and memory-catalog-refresh.sh no longer route _tag_links.md writes to the taxonomy arm — a write to _tag_links.md is ungated inert data, and the gate's bite on _tags.md/_grammar.md is re-proven by the shell battery after the surgery (D-50 critical pitfall 2)"
    - "Every dead-code Info finding in D-51's sweep list is removed: _match_paths(), the unused _add_hit key local, _score_tuples' cfg param, _meets_min_candidate_new's tier_weights param, _apply_score_delta's memdir param, the compile_trigger_index docstring tuple-shape lie, the _PrintSummaryOnSuccess empty class, and the _plant_mark dead translate block — behavior-affecting Info findings stay untouched"
    - "_tag_links.md remains physically in the store as inert data (never deleted) and fingerprint() intentionally keeps its mtime-only entry — exactly one '_tag_links.md' occurrence survives in the engine"
    - "Full pytest suite green and recall p95 ≤55ms re-proven after the read-path-adjacent signature changes (D-57)"
  artifacts:
    - path: "lib/memory_surface.py"
      provides: "Engine with tag-links write path excised + D-51 sweep applied"
    - path: "hooks/memory-write-guard.sh"
      provides: "Taxonomy arm gating _tags.md only (plus the grammar arm)"
    - path: "tests/memory_surface/test_write_hooks.sh"
      provides: "Reworked WR-01 battery: _tags.md gate still bites; _tag_links.md writes provably ungated"
  key_links:
    - from: "hooks/memory-write-guard.sh taxonomy case patterns (lines 72/81/126)"
      to: "lib/memory_surface.py validate()"
      via: "guard calls `python3 ENGINE validate` for taxonomy writes — if validate loses its _tag_links arm while the guard still routes _tag_links writes there, the gate claims to validate what it no longer checks (the half-wired failure mode this plan exists to prevent)"
      pattern: "_tags.md"
    - from: "hooks/memory-catalog-refresh.sh case patterns (lines 67/104)"
      to: "lib/memory_surface.py validate()"
      via: "same lockstep requirement on the PostToolUse side"
      pattern: "TYPE=taxonomy"
    - from: "search() hot path (_add_hit/_score_tuples/_meets_min_candidate_new)"
      to: "tests/memory_surface/bench_recall.sh"
      via: "D-51 signature changes touch the read path → p95 ≤55ms gate must be re-proven (D-57)"
      pattern: "gate=PASS"
---

## Phase Goal

**As a** solo operator of this box's Claude Code harness, **I want to** strip the repo to exactly the components the post-flip core justifies — with the engine, guards, and tests all telling one consistent story — **so that** a fresh session is never misled by a write path that mutates a file nothing reads, or a gate that claims to validate what it no longer checks.

(Recast as a user story from the ROADMAP Phase 4 Goal line; infrastructure phase — each plan is a complete coherent slice.)

This plan delivers the second slice: after it completes, the one routing vocabulary (principle 6) has no shadow write path left, and every dead-code Info finding carried from the phase 2/3 reviews is swept.

<objective>
Excise the _tag_links.md write path from the engine, its CLI, both consuming hooks, and the tests that pinned it — in one lockstep plan with before/after gates (D-50) — then sweep the D-51 dead-code findings and re-prove the suite and the p95 budget.

Purpose: ORG-01 (one source of truth — _grammar.md is the only routing vocabulary with a write path) and ORG-02 (components re-justified: code that nothing consumes is removed, not documented around).
Output: a smaller, truthful engine; lockstep-consistent hooks; a realigned test suite; a fresh bench gate record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reorganization-realignment/04-CONTEXT.md
@.planning/phases/04-reorganization-realignment/04-RESEARCH.md
@.planning/phases/04-reorganization-realignment/04-01-SUMMARY.md
</context>

## Artifacts this phase produces

Full phase inventory lives in 04-01-PLAN.md. This plan produces: the post-D-50 engine + lockstep hooks, the post-D-51 sweep, and the realigned test files (8 dead tests deleted, 1 rewritten, WR-01 battery reworked).

**Standing constraint (D-56):** never edit/stage/move/revert `memory/_grammar.md` or `memory/_tags.md`. `git status --porcelain memory/` must show exactly ` M memory/_grammar.md` and ` M memory/_tags.md` before every commit. Explicit-path staging only. Note: this plan edits ZERO files under `memory/` — `_tag_links.md` stays physically untouched (inert store data per D-50).

**Note on line anchors:** all line numbers below are pre-surgery anchors verified 2026-06-12. Locate by symbol name if drift has occurred; do not guess.

<tasks>

<task type="auto">
  <name>Task 1: D-50 lockstep surgery — engine write path + CLI + both hook case patterns in one move</name>
  <files>lib/memory_surface.py, hooks/memory-write-guard.sh, hooks/memory-catalog-refresh.sh</files>
  <read_first>
    - .planning/phases/04-reorganization-realignment/04-RESEARCH.md ("D-50: parse_tag_links() write-path deletion" — full caller map; "Pattern: Engine Write-Path Surgery"; "Common Pitfalls" 2)
    - lib/memory_surface.py (lines 274–302 parse_tag_links; 444–446 synonym_map; 450–478 validate; 1249–1257 the rebuild comment; 2002–2007 search docstring; 2229–2233 _drop_pair_lines; 2277–2294 add_tag — the mechanical verification target; 2297–2326 link/unlink; 2674–2691 CLI dispatch; lines 1–18 module docstring)
    - hooks/memory-write-guard.sh (lines 10–11 header, 71–79 exemption case, 80–85 TYPE case, 126 sibling-copy loop)
    - hooks/memory-catalog-refresh.sh (lines 66–71 backing-file case, 102–108 TYPE case)
  </read_first>
  <action>
BEFORE gate (record outputs for the SUMMARY): run `python3 -m pytest tests/ -q` (expect 370 passed, from 04-01) and `bash tests/memory_surface/test_write_hooks.sh` (expect exit 0). These prove the starting state so any regression is attributable to this surgery.

MECHANICAL VERIFICATION (orchestrator resolution 2 — do this before touching CLI dispatch): read `add_tag()` at lines 2277–2294 end to end. Confirm it consults only `parse_tags_md(memdir / "_tags.md")` and writes `memdir / "_tags.md"` via `_mutate_then_validate` — no `parse_tag_links`, no `_tag_links.md`. Planning-time verification already confirmed this; re-confirm and record one line in the SUMMARY ("add_tag touches _tags.md only → add-tag CLI arm retained"). If (contrary to expectation) it consults _tag_links.md, that consultation goes with D-50: remove the consultation, keep the _tags.md mutation.

ENGINE SURGERY (lib/memory_surface.py) — delete, in this order:
1. `link()` (lines 2297–2308) and `unlink()` (lines 2311–2326) — entire functions
2. `_drop_pair_lines()` (lines 2229–2233) — its only callers were link/unlink. KEEP `_insert_under_heading()` (add_tag still uses it)
3. CLI dispatch (lines 2674–2691): change `if cmd in ("link", "unlink", "add-tag", "dismiss"):` to `if cmd in ("add-tag", "dismiss"):` and delete the `if cmd == "link":` and `elif cmd == "unlink":` branches; the `add-tag` branch becomes the leading `if`
4. `validate()` (lines 450–478): delete line 452 (`links = parse_tag_links(...)`) and lines 458–477 (the syn_pairs/alias_to_canon synonym block, the distinctions loop, the path_tags loop). What remains: parse_tags_md, the active-tag denylist loop (lines 455–457), `return errors`
5. `parse_tag_links()` (lines 274–302) — now zero callers
6. `synonym_map()` (lines 444–446) — already zero callers (pre-verified)

ENGINE PROSE (same commit — the engine must not lie about itself):
- Module docstring line 7: `validate      check taxonomy (_tags.md + _tag_links.md) integrity` → `validate      check taxonomy (_tags.md) integrity`
- Module docstring line 11: delete the stale `Phase 2 will add: search / link / unlink / add-tag ...` sentence (search and add-tag exist; link/unlink no longer do)
- Module docstring lines 15–17: rewrite the `_review_game.py` frontmatter-mirroring sentence to past tense (the file was deleted in 04-01; the layout it pinned is now pinned by generate_frontmatter + its tests)
- rebuild() comment lines 1253–1254: rewrite — `_tag_links.md` is inert legacy store data; the write path that consumed it was removed in Phase 4 (D-50); synonyms come solely from _grammar.md
- search() docstring line 2006: `NEVER calls parse_tags_md / parse_tag_links — those are write-path only after the flip.` → `NEVER reads _tags.md or any store taxonomy file — catalog only.`
- fingerprint() line 491: KEEP `_tag_links.md` in the mtime tuple (intentional: mtime-only staleness input, content never read; removing it would churn catalog fingerprints for zero punch). After all edits, `grep -c '_tag_links.md' lib/memory_surface.py` must print exactly 1 (that fingerprint line). Do not add new comments naming _tag_links.md.

HOOK LOCKSTEP (same commit as the engine surgery — never leave the taxonomy arm half-wired):
- hooks/memory-write-guard.sh line 72: `_tags.md|_tag_links.md|_grammar.md)` → `_tags.md|_grammar.md)`
- hooks/memory-write-guard.sh line 81: `_tags.md|_tag_links.md) TYPE=taxonomy ;;` → `_tags.md) TYPE=taxonomy ;;` (a _tag_links.md write now falls through to the `_*` arm at line 83 → exit 0, ungated inert data — the intended D-50 end state)
- hooks/memory-write-guard.sh line 126: `for f in _tags.md _tag_links.md _grammar.md; do` → `for f in _tags.md _grammar.md; do`
- hooks/memory-write-guard.sh line 11 header comment: drop `_tag_links.md` from the taxonomy description
- hooks/memory-catalog-refresh.sh line 67: `_tags.md|_tag_links.md|_grammar.md)` → `_tags.md|_grammar.md)`
- hooks/memory-catalog-refresh.sh line 104: `_tags.md|_tag_links.md) TYPE=taxonomy ;;` → `_tags.md) TYPE=taxonomy ;;` (a store-addressed _tag_links.md write still triggers the unconditional rebuild at line 111 — harmless and correct: rebuild ignores the file — but no longer runs the validate arm)

After this task the pytest suite is EXPECTED RED on exactly the 9 tests that pin link/unlink/validate-links behavior (enumerated in Task 2) — that red window exists only inside this plan and closes in Task 2. `python3 -c "import ast; ast.parse(open('lib/memory_surface.py').read())"` must pass now; do not commit the engine without the hook edits.
  </action>
  <verify>
    <automated>python3 -c "import ast; ast.parse(open('lib/memory_surface.py').read())" && ! grep -qE 'def (link|unlink|parse_tag_links|synonym_map|_drop_pair_lines)\(' lib/memory_surface.py && grep -c '_tag_links.md' lib/memory_surface.py | grep -qx 1 && ! grep -q '_tag_links' hooks/memory-write-guard.sh && ! grep -q '_tag_links' hooks/memory-catalog-refresh.sh && grep -q '_tags.md) TYPE=taxonomy' hooks/memory-write-guard.sh && bash -n hooks/memory-write-guard.sh && bash -n hooks/memory-catalog-refresh.sh && python3 lib/memory_surface.py validate && echo D50-LOCKSTEP</automated>
  </verify>
  <acceptance_criteria>
    - BEFORE-gate outputs recorded: `370 passed` and write-hooks battery exit 0
    - add_tag() mechanical verification recorded in the SUMMARY with the retain/remove verdict
    - `grep -cE 'def (link|unlink|parse_tag_links|synonym_map|_drop_pair_lines)\(' lib/memory_surface.py` prints 0
    - `grep -c '_tag_links.md' lib/memory_surface.py` prints exactly 1 (the fingerprint mtime line)
    - `grep -c '_tag_links' hooks/memory-write-guard.sh hooks/memory-catalog-refresh.sh` prints 0 for both files (comments included)
    - `python3 lib/memory_surface.py validate` exits 0 against the live store (validate now checks _tags.md only)
    - Engine + both hooks land in ONE commit (lockstep — D-50 critical constraint)
  </acceptance_criteria>
  <done>The engine, its CLI, and both consuming hooks tell one consistent story: _tags.md and _grammar.md are gated vocabulary; _tag_links.md is inert data with no write path, no validation arm, and no gate that pretends otherwise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test realignment — delete the 8 dead pins, rewrite the policy pin, rework the WR-01 battery (AFTER gate)</name>
  <files>tests/memory_surface/test_phase1.py, tests/memory_surface/test_phase2.py, tests/memory_surface/test_write_hooks.sh</files>
  <read_first>
    - tests/memory_surface/test_phase1.py (lines 180–198: the Validate class; lines 270–280: the stale write-path comment)
    - tests/memory_surface/test_phase2.py (lines 401–430 Mutators; lines 444–540 ReviewRegressions — the link/unlink pins and the _mutate_then_validate policy pins)
    - tests/memory_surface/test_write_hooks.sh (lines 286–328: the WR-01 battery; lines 60–70: fixture setup writing _tag_links.md)
    - lib/memory_surface.py (post-surgery validate() and _mutate_then_validate() — the surviving policy the rewritten test must pin)
  </read_first>
  <behavior>
    Post-D-50 contract this task pins (write the reworked battery/test first, watch it fail against nothing-changed expectations, then confirm green):
    - A Write to the store's _tag_links.md with structurally broken content (e.g. a synonym naming an undefined canonical) is ALLOWED: guard exit 0, no stderr (ungated inert data — the new pin)
    - A Write to the store's _tags.md introducing a NEW error is still DENIED with exit 2 and the error named on stderr (the taxonomy gate still bites — unchanged)
    - A repairing Write of _tags.md over a broken _tags.md is still ALLOWED (pre-existing-error policy survives in the engine via add_tag/_mutate_then_validate and in the guard's pre/errs diff)
    - ms.add_tag against a store whose _tags.md carries a pre-existing denylist error still returns rc 0 for an unrelated valid tag (the _mutate_then_validate "pre-existing unrelated errors must not block" policy, now pinned via _tags.md instead of _tag_links.md)
  </behavior>
  <action>
DELETE these 9 tests — each pins a function or validation arm that no longer exists (deletions remove their dead tests in the same plan as the code they pinned):
- test_phase1.py: `Validate.test_synonym_canonical_must_be_active` (lines 192–197)
- test_phase2.py: `Mutators.test_link_ok` (422–424), `Mutators.test_link_fail_closed_rolls_back` (426–430), `ReviewRegressions.test_link_removes_existing_distinction` (496–499), `ReviewRegressions.test_unlink_distinguish_removes_existing_synonym` (501–506), `ReviewRegressions.test_freetext_reason_cannot_inject_taxonomy` (508–516), `ReviewRegressions.test_multiple_synonym_set_rejected` (518–522), `ReviewRegressions.test_mutator_blocks_duplicate_error_edit` (532–539)
(That is 8 pytest-collected deletions; the 9th red item is the rewrite below.)

REWRITE `ReviewRegressions.test_mutator_ignores_preexisting_unrelated_error` (524–530): the policy it pins (_mutate_then_validate ignores pre-existing errors) is alive — only its error-injection vehicle died. Replace the broken-_tag_links.md setup with a broken-_tags.md setup: write a fixture `_tags.md` where an active tag also appears in the Denylist without a Policy override (e.g. append `- config — wrongly active` under `## domain`, mirroring the test_phase1.py line 186 pattern), assert `ms.validate(self.store)` is non-empty, then `ms.add_tag(self.store, "freshtag", "a perfectly fine six word description here", "tool")` must return rc 0.

COMMENT REALIGNMENT: test_phase1.py lines 274–276 say _tag_links.md synonyms are "now write-path only" — update to "inert legacy data; no write path since Phase 4 (D-50)". Leave all `_tag_links.md` FIXTURE writes across the suite in place (test_phase1.py:90, test_phase2.py:198, test_dedup_placement.py, test_write_triggers.py:633, test_routing_contract.py, test_phase3.py): real stores also retain the file, fixtures mirroring reality is correct, and rebuild()/validate() now ignore it either way. test_routing_contract.py M08 (structural-independence) keeps working unchanged.

REWORK the test_write_hooks.sh WR-01 battery (lines 286–328) to pin the post-D-50 guard contract per the behavior block:
- Keep: break `_tags.md` (the WR01_TAGS_EOF heredoc), then a repairing Write of GOOD_TAGS_SNAPSHOT → rc 0
- Replace the `_tag_links.md` repair/new-error assertions (lines 313–326) with the new ungated pin: Write the WR01_LINKS_EOF broken-synonym content to `$FIX/_tag_links.md` via the guard → expect rc 0 AND empty stderr, labeled e.g. `rc_is "GUARD _tag_links.md write ungated (inert data, D-50) -> rc=0" 0 "$got_rc"`
- Keep a NEW-error `_tags.md` deny assertion so the battery still proves the gate bites: Write a _tags.md adding a second active denylisted tag (new error vs pre) → rc 2, stderr names it. Restore snapshots at the end (lines 327–328 pattern)
- Update the battery's section header comments to describe the post-D-50 contract

AFTER gate (the lockstep proof, recorded for the SUMMARY):
- `python3 -m pytest tests/ -q` → exit 0, exactly 362 collected (370 − 8 enumerated deletions; the rewrite keeps its slot). Any other delta = something was lost silently → STOP and fix
- `bash tests/memory_surface/test_write_hooks.sh` → exit 0 with the new D-50 assertion lines visible in output
- Note: tests/memory_surface/test_hooks_phase1.sh retains its 2 pre-existing failures tied to the other session's uncommitted taxonomy edits — out of scope per D-57; do not chase them, do not let them grow beyond 2
  </action>
  <verify>
    <automated>python3 -m pytest tests/ -q && python3 -m pytest tests/ --collect-only -q 2>/dev/null | tail -1 | grep -q '362 tests collected' && bash tests/memory_surface/test_write_hooks.sh && grep -q 'ungated' tests/memory_surface/test_write_hooks.sh && ! grep -q 'ms.link\|ms.unlink' tests/memory_surface/test_phase2.py && echo D50-GATES-GREEN</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 362 tests collected, all passing (370 − 8; delta equals the enumerated deletion list)
    - The rewritten policy pin passes and still exercises _mutate_then_validate's pre-existing-error subtraction (via _tags.md)
    - test_write_hooks.sh exits 0 and its output includes the new `_tag_links.md write ungated` PASS line AND a `_tags.md` deny PASS line (gate still bites both directions)
    - `grep -c 'ms\.link\|ms\.unlink' tests/memory_surface/test_phase2.py` prints 0
    - test_hooks_phase1.sh failure count is still exactly the 2 pre-existing ones (D-57 boundary held)
  </acceptance_criteria>
  <done>The suite pins the post-D-50 world: the taxonomy gate provably still bites on _tags.md, _tag_links.md writes are provably ungated, and no test references a deleted function.</done>
</task>

<task type="auto">
  <name>Task 3: D-51 dead-code sweep + full-suite and p95 bench gates</name>
  <files>lib/memory_surface.py, tests/memory_surface/test_probe_runner.py, tests/memory_surface/test_phase3.py</files>
  <read_first>
    - .planning/phases/04-reorganization-realignment/04-RESEARCH.md ("D-51: Dead code/unused params sweep" — the sweep table AND the explicit skip-list of behavior-affecting Info findings)
    - lib/memory_surface.py (lines 562–581 compile_trigger_index docstring; 822–839 _apply_score_delta + callers at 1025/1029; 983 and 1144 minEvidenceSessions reads; 1899–1931 _match_paths; 1934–1960 _score_tuples + caller at 2183; 1986–1999 _meets_min_candidate_new + caller at 2181; 2042–2048 _add_hit)
    - tests/memory_surface/test_probe_runner.py (lines 476–494: _PrintSummaryOnSuccess and _run_with_summary — confirm the class has zero references before deleting)
    - tests/memory_surface/test_phase3.py (lines 262–273: _plant_mark — the dead translate block at 266–267 vs the live re.sub at 269–270)
  </read_first>
  <action>
Sweep EXACTLY the dead-code/pruning-class items; the research's behavior-affecting Info findings (IN-03..IN-10 phase 2; IN-04..IN-12 phase 3 except the two below) are explicitly out of scope per D-51 — touch nothing else.

Engine (lib/memory_surface.py):
1. Delete `_match_paths()` (lines 1899–1931) — zero callers; search() inlines its own byPath loop
2. `_add_hit` (line 2043): delete the unused `key = (tag, trigger_type)` local
3. `_score_tuples(tuples, mem, cfg, now, tier_weights)` (line 1934): drop the unused `cfg` param → `_score_tuples(tuples, mem, now, tier_weights)`; update the single caller at line 2183
4. `_meets_min_candidate_new(tuples, tier_weights)` (line 1986): drop the unused `tier_weights` param → `_meets_min_candidate_new(tuples)`; update the single caller at line 2181
5. `_apply_score_delta(p, memdir, direction)` (line 822): drop the unused `memdir` param → `_apply_score_delta(p, direction)`; update both callers (lines 1025, 1029); while editing the docstring, rewrite its `_review_game.py's deprecated writer` reference to past tense (file deleted 04-01)
6. compile_trigger_index docstring (line 567): fix the memories_meta shape lie — it documents `(stem, meta, body_text)` 3-tuples but the unpack loop and rebuild() build `(stem, meta, name, description, body_text)` 5-tuples
7. minEvidenceSessions (lines 983, 1144): add the IN-11 clarifying comment at the config-read sites noting the companion key `minEvidenceDays` carries the calendar-days arm and the window is `sessions OR days` — comment only, no behavior change

Tests:
8. test_probe_runner.py: delete the empty `_PrintSummaryOnSuccess` class (lines 480–482) and its orphaned section comment if it then describes nothing; `_run_with_summary` does not reference it (pre-verified) — confirm with grep before deleting
9. test_phase3.py `_plant_mark` (lines 266–267): delete the dead `safe = stem.translate(...)` computation whose result is immediately overwritten by the `re.sub` at line 270 (IN-02 phase 3 — dead code in a test file, in-scope for D-51); KEEP the local `import re` (no module-level re import exists in that file)

Gates (wave exit, D-57 — the signature changes in items 3/4 and the `_add_hit` edit sit in the search() hot path, so the bench is mandatory):
- `python3 -m pytest tests/ -q` → exit 0, 362 collected
- `bash tests/memory_surface/test_write_hooks.sh` → exit 0
- `bash tests/memory_surface/bench_recall.sh` → output contains `gate=PASS` (p95 ≤ 55ms); record the full samples/p50/p95 lines verbatim in the SUMMARY
  </action>
  <verify>
    <automated>! grep -q 'def _match_paths(' lib/memory_surface.py && ! grep -qE '_score_tuples\(.*cfg' lib/memory_surface.py && ! grep -q '_PrintSummaryOnSuccess' tests/memory_surface/test_probe_runner.py && ! grep -q 'str.maketrans' tests/memory_surface/test_phase3.py && python3 -m pytest tests/ -q && bash tests/memory_surface/test_write_hooks.sh && bash tests/memory_surface/bench_recall.sh | grep -q '^gate=PASS' && echo D51-SWEPT</automated>
  </verify>
  <acceptance_criteria>
    - All 9 sweep items applied; `grep -c 'def _match_paths\|_PrintSummaryOnSuccess' lib/memory_surface.py tests/memory_surface/test_probe_runner.py` prints 0 for both
    - `_score_tuples` and `_meets_min_candidate_new` and `_apply_score_delta` callers updated — pyright/LSP reports no new diagnostics on lib/memory_surface.py
    - No behavior-affecting Info finding was touched (spot-checkable: surface_text truncation, byCommand synonym labeling, seats pending-block logic all unchanged)
    - `python3 -m pytest tests/ -q` → 362 passed, exit 0
    - `bash tests/memory_surface/bench_recall.sh` prints `gate=PASS` with p95 ≤ 55ms; full output recorded in SUMMARY
    - `git status --porcelain memory/` unchanged (D-56)
  </acceptance_criteria>
  <done>Every carried dead-code finding is gone, nothing behavioral moved, and the read path is re-proven inside budget — the engine earns its size again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard/refresh hooks → engine validate | The hooks gate live writes by calling the engine; a desynced taxonomy arm silently no-ops the write gate (claims validation that never happens) |
| repo engine → live recall path | lib/memory_surface.py is LIVE via the hooks' relative resolution; a syntactically broken intermediate state degrades live recall (fail-open = silent) until fixed |
| read-path signatures → p95 budget | _score_tuples/_meets_min_candidate_new/_add_hit changes sit in the per-tool-call hot path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Spoofing | guard claiming to validate _tag_links.md | mitigate | Lockstep: engine + both hooks in ONE commit; battery pins both directions (deny on _tags.md new-error, allow on _tag_links.md) |
| T-04-07 | Denial of Service | live engine broken mid-surgery | mitigate | ast.parse gate before commit; fail-open posture bounds blast radius to silent recall; red window confined to within this plan |
| T-04-08 | Tampering | sweep drifting into behavior-affecting findings | mitigate | Sweep list is closed-form (9 items); research skip-list named in read_first; acceptance spot-checks unchanged behaviors |
| T-04-09 | Denial of Service | p95 regression from hot-path edits | mitigate | bench_recall.sh gate=PASS mandatory at wave exit (D-57); verbatim record in SUMMARY |
| T-04-10 | Repudiation | silent test loss during realignment | mitigate | Exact collected-count gates (370 → 362) with the delta tied to the enumerated deletion list |
| T-04-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- BEFORE gates recorded (370 passed, battery exit 0); AFTER gates: 362 passed, battery exit 0 with new D-50 pins, `gate=PASS` bench
- `grep -c '_tag_links' hooks/memory-write-guard.sh hooks/memory-catalog-refresh.sh` → 0/0; `grep -c '_tag_links.md' lib/memory_surface.py` → exactly 1
- No `def link(`/`def unlink(`/`def parse_tag_links(`/`def synonym_map(`/`def _drop_pair_lines(` in the engine; CLI rejects `link`/`unlink` as unknown commands
- test_hooks_phase1.sh still at exactly its 2 pre-existing failures (D-57 boundary)
- `git status --porcelain memory/` → only the two D-56 lines
</verification>

<success_criteria>
- D-50 complete and lockstep-consistent: no half-wired taxonomy arm anywhere; _tag_links.md is inert store data with exactly one (intentional, mtime-only) engine reference
- D-51 swept exactly: all dead-code items gone, all behavior-affecting Info findings untouched
- 362/362 green, write-hook battery green, recall p95 ≤55ms re-proven (D-57)
</success_criteria>

<output>
Create `.planning/phases/04-reorganization-realignment/04-02-SUMMARY.md` when done. Record: the add_tag mechanical-verification verdict, the BEFORE/AFTER gate outputs (counts + battery), the bench_recall.sh output verbatim, and the lockstep commit hash carrying engine + both hooks together.
</output>
