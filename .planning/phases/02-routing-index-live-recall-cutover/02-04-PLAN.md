---
phase: 02-routing-index-live-recall-cutover
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - lib/memory_surface.py
  - memory/_tags.md
  - memory/_tag_links.md
  - tests/memory_surface/test_phase2.py
  - tests/memory_surface/test_probe_runner.py
  - .planning/MVR.md
autonomous: true
requirements: [MIG-02, CORE-03, CORE-04, CORE-05, CORE-06, CORE-09]
must_haves:
  truths:
    - "Every .planning/MVR.md checklist item is checked by a real demonstration run, recorded with its command and output — before the flip commit exists"
    - "After the flip, the live hook's `search` call routes through the trigger index; the engine holds exactly one routing implementation (principle 6)"
    - "grep confirms no read-path code consumes _tags.md or _tag_links.md; both carry a legacy header and validate clean"
    - "Rollback is two moves: git revert of the single flip commit + .surface-disabled kill-switch"
    - "No window where old memories are unreachable: the flip lands only after probes prove the new path routes the live store"
  artifacts:
    - path: ".planning/MVR.md"
      provides: "All checklist boxes checked with demonstration evidence"
      contains: "[x]"
    - path: "lib/memory_surface.py"
      provides: "search() IS the trigger-index matcher; legacy search internals, score_memory tag-category scoring, search-new subcommand, and env dispatch all pruned"
      contains: "triggerIndex"
    - path: "memory/_tags.md"
      provides: "Legacy header comment (historical reference, not a routing input)"
      contains: "LEGACY"
    - path: "memory/_tag_links.md"
      provides: "Legacy header comment (historical reference, not a routing input)"
      contains: "LEGACY"
  key_links:
    - from: "hooks/memory-recall.sh (unchanged file)"
      to: "search() new internals"
      via: "the engine swap happens under the same subcommand name — the hook never changes (D-28/D-30)"
      pattern: "python3 \"\\$ENGINE\" search"
    - from: ".planning/MVR.md"
      to: "demonstration commands"
      via: "each checked box cites the run that proved it"
      pattern: "Demonstrated by"
---

## Phase Goal

**As a** Claude session making tool calls on this box, **I want to** have the right memories surface from a near-free precomputed evidence lookup that explains every fire, **so that** recall is trustworthy, diagnosable in seconds, and the legacy routing path can be removed with every existing memory still reachable.

This plan is the gate and the cut: after it completes, live recall on this box RUNS the reimagined system — every previous slice becomes the production path, and the legacy path no longer exists.

<objective>
Run every MVR demonstration as a real command (MIG-01 gate close), then execute the staged flip in ONE commit (D-30): `search_new` becomes `search`, legacy routing code is pruned (D-31), `_tags.md`/`_tag_links.md` get legacy headers (MVR removal steps 1–4), and post-flip verification proves the live path fires with tuples at budget.

Purpose: MIG-02 (cutover with no dark window), CORE-03..06/09 close-out, MVR gate satisfaction — the phase's spine.
Output: checked MVR.md, the flip commit, pruned engine, legacy-marked taxonomy files, post-flip verification record.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MVR.md
@.planning/phases/02-routing-index-live-recall-cutover/02-CONTEXT.md
@.planning/phases/02-routing-index-live-recall-cutover/02-RESEARCH.md
@.planning/phases/02-routing-index-live-recall-cutover/02-PATTERNS.md
@.planning/phases/02-routing-index-live-recall-cutover/02-03-SUMMARY.md
</context>

## Artifacts this phase produces

This plan creates/changes (full phase inventory lives in 02-01-PLAN.md):

- `lib/memory_surface.py::search()` — the trigger-index matcher under the canonical name (was `search_new`)
- Removed symbols: legacy `search()` internals, `score_memory()` and `_CAT_PRIORITY`/legacy `_meets_min_candidate()` (tag-category scoring), `parse_tag_links()` if zero callers remain, the `search-new` CLI branch, the `MEMORY_SURFACE_SEARCH_IMPL` env dispatch
- `rebuild()` canonicalization source switches from `parse_tag_links` synonyms to grammar synonyms (one routing vocabulary)
- Legacy header comments in `memory/_tags.md` and `memory/_tag_links.md`
- `.planning/MVR.md` — all boxes checked with evidence; status flipped to CLOSED
- Updated tests: legacy-scoring assertions in `tests/memory_surface/test_phase2.py` retired/rewritten; `test_probe_runner.py` drops the env dispatch

<tasks>

<task type="auto">
  <name>Task 1: Run every MVR demonstration and check the boxes (pre-flip)</name>
  <files>.planning/MVR.md</files>
  <read_first>
    - .planning/MVR.md (the gate — each item names its demonstration; this task executes them verbatim)
    - .planning/phases/02-routing-index-live-recall-cutover/02-03-SUMMARY.md (exact probe/bench commands and last-known numbers)
    - hooks/memory-write-context.sh (the fail-open item also names this hook — know its gate order before testing it)
  </read_first>
  <action>
Execute each MVR demonstration as a REAL run (checking a box requires a run, not a review — MVR's own rule). All runs use the staged path (`MEMORY_SURFACE_SEARCH_IMPL=new` where the hook/engine is involved) against the live store, isolated XDG_RUNTIME_DIR for anything touching dedup marks. For each item, check its box in `.planning/MVR.md` and append a one-line evidence note (command + headline result + date) under the item:

1. **Routability:** `python3 lib/memory_surface.py rebuild` → stderr has no `UNROUTABLE` line; `jq '.routabilityReport.unroutableCount' ~/.claude/projects/-home-jangmanj/memory/_memory_catalog.json` prints 0.
2. **Probes both directions:** `PROBE_LIVE=1 python3 tests/memory_surface/test_probe_runner.py` → 5/5 fire with tuples, 5/5 silent.
3. **p95 ≤ 50ms:** `bash tests/memory_surface/bench_recall.sh -n 20` → `gate=PASS`, quote the `p95_ms=` value.
4. **Tuple in every block:** the probe runner's per-block assertion output (every `<memory-recall` block carries `←` with {tag, trigger_type, matched_value}; no tag named without a firing trigger).
5. **Cold-state rebuild:** back up the live `_memory_catalog.json` (cp to a temp path), DELETE it, run `python3 lib/memory_surface.py rebuild`, then re-run the live probes → both directions pass again. Note: while the catalog is absent, live recall fails closed-silent (advisory posture, seconds-long, acceptable); do the delete→rebuild back-to-back. Remove the backup after success.
6. **Fail-open `.surface-disabled`:** in a FIXTURE store (MEMORY_SURFACE_DIR override — the live kill-switch stays unset), create `.surface-disabled`; sample-JSON stdin runs against `hooks/memory-recall.sh` AND `hooks/memory-write-context.sh` each exit 0 with empty stdout and empty stderr.
7. **Kill-switch / infra-fault:** fixture store WITHOUT `_memory_catalog.json` → `memory-recall.sh` stdin run exits 0, no output, never rc 2.

Item 8 (old-path removal steps) is executed by Task 2 — leave its box for then. If ANY item fails, STOP: do not proceed to Task 2; fix within the prior plans' pinned semantics or surface the failure as a blocker. The flip is forbidden until all of items 1–7 are checked (MIG-01: the legacy path stays live until the gate passes).
  </action>
  <verify>
    <automated>grep -c "^- \[x\]" .planning/MVR.md | grep -qE "^[7-9]" && PROBE_LIVE=1 python3 tests/memory_surface/test_probe_runner.py && echo GATE-ITEMS-1-7-DEMONSTRATED</automated>
  </verify>
  <acceptance_criteria>
    - MVR.md shows items 1–7 checked, each with an appended evidence line naming the command and result
    - The cold-rebuild sequence is recorded (delete → rebuild → probes pass)
    - Fail-open runs show literal empty output: evidence lines quote exit 0 and byte-empty stdout/stderr
    - The flip has NOT happened yet: `grep -c "def search_new" lib/memory_surface.py` still ≥1
  </acceptance_criteria>
  <done>Items 1–7 of the MVR gate are demonstrably satisfied by real runs, recorded in MVR.md, with the legacy path still live.</done>
</task>

<task type="auto">
  <name>Task 2: THE FLIP — one commit: new matcher under `search`, legacy path pruned, removal steps 1–4</name>
  <files>lib/memory_surface.py, memory/_tags.md, memory/_tag_links.md, tests/memory_surface/test_phase2.py, tests/memory_surface/test_probe_runner.py</files>
  <read_first>
    - lib/memory_surface.py (current dispatch: `search` CLI branch with env dispatch, `search-new` branch, legacy search() at ~line 1178, score_memory ~line 1009, rebuild's parse_tag_links smap use at ~line 510)
    - .planning/MVR.md (removal steps 1–4 — the exact ordered list with per-step verification)
    - .planning/phases/02-routing-index-live-recall-cutover/02-PATTERNS.md ("legacy header comment" section — the header text and post-insertion grep verification)
    - tests/memory_surface/test_phase2.py (identify which tests pin legacy scoring/search semantics — they retire with the code they pin)
    - hooks/memory-write-guard.sh (taxonomy arm — D-31 disposition audit in step 6 below)
  </read_first>
  <action>
Stage ALL of the following, verify green, then commit as ONE commit (D-30: reviewable, revertable; rollback = `git revert` + `.surface-disabled`). Hooks are live via symlink — the engine swap takes effect on the next tool call the moment the file is saved, so order the work: edit, run the FULL verification battery, only then commit. If anything breaks mid-edit, `touch <live-store>/.surface-disabled` is the abort lever (remove after recovery).

Engine (`lib/memory_surface.py`):
1. Delete the legacy `search()` implementation; rename `search_new` to `search` (keep the signature `search(memdir, event, now=None)` — the hook and CLI call shape is unchanged, D-28).
2. Remove the `search-new` CLI branch and the `MEMORY_SURFACE_SEARCH_IMPL` env dispatch (temporary scaffolding per D-30).
3. Prune legacy routing code (D-31): `score_memory()`, `_CAT_PRIORITY`, the cats-based `_meets_min_candidate()` and cats-based `_confidence()` (the tuple-based `_score_tuples`/`_confidence_new` are the only scorers now), and the distinctions-conflict logic (grammar has no distinctions field by design, D-03).
4. Switch `rebuild()` canonicalization: the synonym map for `canonicalTags`/`tagToMemoryIds` now comes from grammar synonyms (the same source as `recallVocab.aliases`) instead of `parse_tag_links()` — one routing vocabulary (principle 6). `_tags.md` remains ONLY as the memory-tag validation vocabulary in rebuild/validate/check_write/add_tag (write-path, retires in a later phase — record this boundary in the SUMMARY).
5. `grep -rn "parse_tag_links" lib/ memory/ hooks/ tests/` — if zero non-test callers remain, delete the function; if `memory/_review_game.py` still calls it, keep the function, ensure zero callers on the search path, and record the deferred deletion. `parse_tags_md` STAYS (write-path consumers).
6. Audit `hooks/memory-write-guard.sh` arms per D-31: the taxonomy arm validates `_tags.md`/`_tag_links.md` writes — these files still serve the write path (vocabulary), so the arm is NOT "solely legacy-routing" and STAYS; record this disposition explicitly (the arm retires only when the files do).

Taxonomy files (MVR step 4): prepend the legacy header HTML comment from 02-PATTERNS.md ("LEGACY: ... no longer a routing input after the Phase 2 flip ... retained as historical reference ... index is compiled from memory/_grammar.md and per-memory triggers") to `memory/_tags.md` and `memory/_tag_links.md`. These are live store symlink targets — the write-guard taxonomy arm will validate the edit; `python3 lib/memory_surface.py validate` must still exit 0 after insertion.

Tests:
7. Retire/rewrite tests pinning the deleted legacy semantics (grep test files for `score_memory` and direct legacy-search behavior assertions): delete those superseded by `test_routing_contract.py`; rewrite any still-valuable envelope/response tests against the new `search`. Write-path, dedup, grammar, placement, review-game tests stay untouched.
8. `test_probe_runner.py`: drop `MEMORY_SURFACE_SEARCH_IMPL` from the env (the canonical `search` IS the new path now); fixture + live probes must pass through the unchanged hook.

Removal-step verifications (MVR steps 1–3, run before committing):
- Step 1: live probes pass through `hooks/memory-recall.sh` with NO env override (new path under the canonical name); `grep -c "search_new\|MEMORY_SURFACE_SEARCH_IMPL" lib/memory_surface.py` prints 0 (old staging gone).
- Step 2: the `search()` function body greps clean of `parse_tags_md` and `_tags.md` — extract the body with sed between `def search(` and the next top-level `def`, filter comment lines with `grep -v '^\s*#'`, then count matches: must be 0 (no routing read of `_tags.md`).
- Step 3: same body check for `parse_tag_links` / `_tag_links.md` prints 0; `grep -rn "_tag_links" hooks/` shows no read-path hook consumer.

Full battery before the single commit: `python3 tests/memory_surface/test_routing_contract.py` && full unittest discovery && fixture probes && live probes && `bash tests/memory_surface/bench_recall.sh -n 20` showing `gate=PASS` && `python3 lib/memory_surface.py validate` exit 0 && `python3 lib/memory_surface.py rebuild` 0-unroutable. Then ONE commit containing the engine, taxonomy headers, and test changes; check MVR item 8's box with the removal-step verification evidence.
  </action>
  <verify>
    <automated>grep -c "search_new\|MEMORY_SURFACE_SEARCH_IMPL" lib/memory_surface.py | grep -qx 0 && python3 tests/memory_surface/test_routing_contract.py && python3 -m unittest discover -s tests/memory_surface -p 'test_*.py' 2>&1 | tail -1 && python3 tests/memory_surface/test_probe_runner.py && PROBE_LIVE=1 python3 tests/memory_surface/test_probe_runner.py && bash tests/memory_surface/bench_recall.sh -n 20 | grep -qx "gate=PASS" && head -3 memory/_tags.md | grep -q "LEGACY" && python3 lib/memory_surface.py validate && echo FLIPPED</automated>
  </verify>
  <acceptance_criteria>
    - One commit contains the flip (engine + taxonomy headers + test retirement); `git show --stat HEAD` lists lib/memory_surface.py, memory/_tags.md, memory/_tag_links.md and the test files together
    - `grep -c "search_new\|MEMORY_SURFACE_SEARCH_IMPL\|score_memory" lib/memory_surface.py` prints 0
    - search() body greps clean of parse_tags_md/parse_tag_links/_tags.md/_tag_links.md after comment filtering (removal steps 2–3 verified — grep-gate hygiene: filter `#` lines so prose cannot self-invalidate the check)
    - Both taxonomy files open with the LEGACY header; `python3 lib/memory_surface.py validate` exits 0
    - Live probes pass with NO env override and `bench_recall.sh` reports `gate=PASS` post-flip
    - MVR item 8 checked with per-step evidence lines
  </acceptance_criteria>
  <done>The reimagined matcher is the one and only routing implementation, the legacy path is gone in a single revertable commit, and every MVR removal step is verified.</done>
</task>

<task type="auto">
  <name>Task 3: Post-flip live soak and gate-close record</name>
  <files>.planning/MVR.md</files>
  <read_first>
    - .planning/MVR.md (final state check — every box must now be [x])
    - .planning/phases/02-routing-index-live-recall-cutover/02-CONTEXT.md (phase success criteria 1–5 — the claims this record must support)
  </read_first>
  <action>
Prove the flipped system live, then close the gate record:

1. Live-path spot checks through the REAL hook exactly as a session would hit it (no MEMORY_SURFACE_DIR override, no env overrides, isolated XDG_RUNTIME_DIR via mktemp -d): the F1 payload (`Bash nvidia-smi`) fires with a `←` tuple in `.hookSpecificOutput.additionalContext`; the S1 payload (`Bash ls -la`) is silent; a Write payload targeting a path under the live store is silent (write-skip gate intact); malformed stdin exits 0 silent.
2. Consistency loop closed post-flip: in a FIXTURE store, write to `_grammar.md` via a sample-JSON stdin run against `hooks/memory-catalog-refresh.sh` and confirm the catalog's `triggerIndex` reflects the change (the 02-01 grammar arm still works with the flipped engine). On a fixture, assert `fingerprint()` changes when a memory file's mtime is bumped via os.utime — never utime live files.
3. Update `.planning/MVR.md` header: flip the top status line from OPEN to CLOSED, citing the flip commit hash and date (the gate document itself records that the old routing path was removed only after every box was checked — MIG-01/MIG-02 closure, ROADMAP success criterion 5: no window where old memories were unreachable).
4. Record rollback readiness in the SUMMARY: the flip commit hash and the two-step rollback (`git revert <hash>` then `touch <live-store>/.surface-disabled` while reverting, remove after).
5. Commit the MVR.md gate-close as a docs commit (separate from the flip commit — the flip stays minimal and revertable).
  </action>
  <verify>
    <automated>grep -c "^- \[ \]" .planning/MVR.md | grep -qx 0 && grep -qi "CLOSED" .planning/MVR.md && printf '{"tool_name":"Bash","tool_input":{"command":"nvidia-smi"},"cwd":"/tmp"}' | XDG_RUNTIME_DIR=$(mktemp -d) bash hooks/memory-recall.sh | jq -e '.hookSpecificOutput.additionalContext | contains("memory-recall")' >/dev/null && echo GATE-CLOSED</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/MVR.md` has zero unchecked boxes and a CLOSED status line citing the flip commit hash
    - Live F1 spot check emits a block containing `←`; live S1 spot check emits nothing; both exit 0
    - Fixture grammar-write refresh run shows the catalog triggerIndex updating post-flip
    - SUMMARY records: rollback recipe, the final p95 number, the routability count, and the write-path boundary note (_tags.md vocabulary role retained for a later phase)
    - Gate-close is its own docs commit, distinct from the flip commit
  </acceptance_criteria>
  <done>The reimagined read path is live, demonstrated end-to-end on the real box, and the MVR gate is closed with a complete evidence trail and a two-step rollback recipe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flip commit → live read path | The engine swap is live the moment the file saves (hooks via symlink) — the riskiest moment of the phase |
| taxonomy file edits → write-guard | Legacy headers are writes to guarded, symlinked store files |
| MVR record → removal decision | The gate evidence authorizes destruction of the old path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Denial of Service | live recall during the flip edit window | mitigate | Full battery green before commit; `.surface-disabled` kill-switch as abort lever; advisory posture means worst case is silence, never a blocked tool call |
| T-02-17 | Tampering | legacy header insertion into guarded taxonomy files | mitigate | Write-guard taxonomy arm validates the proposed content (WR-04 temp-store check); `validate` exit-0 asserted post-insertion; HTML comment form chosen because parsers ignore non-matching lines |
| T-02-18 | Repudiation | gate evidence | mitigate | Every checked box carries its command + result + date; flip and gate-close are separate commits so git history shows gate-before-flip ordering |
| T-02-19 | Denial of Service | cold-rebuild demonstration deletes the live catalog | mitigate | Backup copy first; delete→rebuild executed back-to-back; recall fails closed-silent (advisory) during the seconds-long gap; restore-from-backup path if rebuild fails |
| T-02-20 | Elevation of Privilege | rollback path | accept | `git revert` + kill-switch are user-initiated local operations; no permission surface touched (harness invariant: no permissions writes, ever) |
| T-02-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- Pre-flip: MVR items 1–7 checked with evidence; legacy path still live
- Flip: one commit; grep-verified removals; full battery green; taxonomy headers validate clean
- Post-flip: live hook fires with tuples and stays silent correctly with zero env overrides; benchmark `gate=PASS`; MVR CLOSED
</verification>

<success_criteria>
- Old routing path removed ONLY after every MVR item was demonstrated by a real run (MIG-01 close; ROADMAP criterion 5)
- All ~144 store memories routable at cutover with no dark window (MIG-02)
- Engine holds exactly one routing implementation; `_tags.md`/`_tag_links.md` are grep-verified inert on the read path and marked legacy (D-31, principle 6)
- Live per-tool-call recall: precomputed, explainable, silent-by-default, fail-open, ≤50ms p95 (CORE-04/05/06)
- Rollback: git revert + `.surface-disabled` documented and ready (D-30)
</success_criteria>

<output>
Create `.planning/phases/02-routing-index-live-recall-cutover/02-04-SUMMARY.md` when done. Record: the flip commit hash, final p95, routability count, removal-step grep evidence, the write-guard arm disposition (stays — write-path role), the parse_tag_links deletion-or-deferral outcome, and the rollback recipe.
</output>
