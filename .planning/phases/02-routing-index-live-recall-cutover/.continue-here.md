---
context: phase
phase: 02-routing-index-live-recall-cutover
task: 7
total_tasks: 12
status: in_progress
last_updated: 2026-06-12T10:05:00.000Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| probe-dedup-masking | Recall dedup marks (15-min TTL at $XDG_RUNTIME_DIR/claude-memory-recall/m_*) silently suppress repeat probe fires — a probe run can pass/fail depending on invisible mark state | advisory | 02-03 plan already encodes it: probes and benchmark run under a per-run temp XDG_RUNTIME_DIR; never assert fire/silence against the live session's mark dir |
| live-symlink-blindness | Hooks (and the engine they resolve) are LIVE on this box via ~/.claude/hooks symlinks; engine/hook edits on the main tree take effect on the next tool call of ANY session | advisory | D-18/D-30 ordering encoded in plans: tests green before dependent hook edits; new matcher stays behind `search-new` subcommand + MEMORY_SURFACE_SEARCH_IMPL env dispatch until the 02-04 flip; `.surface-disabled` is the kill-switch |

**Severity values:** `blocking` — resuming agent must pass understanding check before proceeding. `advisory` — important context, does not gate resumption.

<current_state>
/gsd-autonomous run, paused cleanly at the wave 2 → wave 3 boundary of Phase 2 execution (2 of 4 plans complete, working tree clean, nothing in flight).

Phase 2 was discussed (smart discuss — all recommended answers accepted, D-21..D-32 in 02-CONTEXT.md), researched (02-RESEARCH.md), pattern-mapped (02-PATTERNS.md), planned (4 plans, plan-checker passed iteration 1, all coverage gates green). Execution is sequential on the main working tree (worktree isolation auto-degraded per #683 — HEAD diverged from origin/HEAD; local commits unpushed).

Wave 1 (02-01) COMPLETE: triggerIndex compiled into the live catalog (byCommand/byPath/byArg/bySynonym/byMemoryId), routabilityReport {unroutableCount: 0} on the live store (144 memories: 134 grammar-covered + 10 D-29b mechanical fallback), fingerprint() now hashes _grammar.md, refresh-hook mutation gaps closed.

Wave 2 (02-02) COMPLETE (finished in background after pause was requested; verified: SUMMARY on disk, tree clean): search_new() trigger-index matcher with TIER_WEIGHTS scoring, confidence gating, inline `{tag} ← {trigger_type}:{matched_value}` tuples, CLI `search-new` subcommand + MEMORY_SURFACE_SEARCH_IMPL=new env dispatch. 289/289 tests green (57 contract). Live-store smoke: 3 fire payloads returned ← tuples, silence payload empty. In-process 0.84ms p95. Legacy search() and hooks/ byte-untouched.
</current_state>

<completed_work>

Completed this session:
- Phase 1 leftovers committed (review iter-2 artifacts + live _tags.md addition) — b0ba864
- 02-CONTEXT.md via autonomous smart discuss (3 grey areas, operator accepted all) — aaaeca7
- 02-RESEARCH.md (key flags: full-hook p95 baseline 52–59ms vs ≤50ms gate, 11 fallback memories, fingerprint gap, dedup-mark probe trap, empty byArg at v0)
- 02-PATTERNS.md (6/6 analogs)
- 4 plans (02-01..02-04) + checker pass + gates; committed 92cf3a2, D-24 informational fix b72bed8
- Plan 02-01 executed: 90b6780, 305f51a, 3038994, 37c071c; 02-01-SUMMARY.md
- Plan 02-02 executed: d0eac89 (RED), 0fee44a (GREEN), 42739b2 (docs); 02-02-SUMMARY.md
</completed_work>

<remaining_work>

- 02-03 (wave 3): probe runner — ≥5 obvious-should-fire + ≥5 obvious-should-stay-silent synthetic PreToolUse payloads through the REAL hooks/memory-recall.sh (isolated per-run XDG_RUNTIME_DIR, dedup-mark clearing); bench_recall.sh p95 benchmark (≥20 samples, gate ≤50ms); jq-reduction lever in memory-recall.sh if over budget
- 02-04 (wave 4): run every .planning/MVR.md demonstration for real; if ALL pass → ONE flip commit (search → new matcher, MVR removal steps 1–4, legacy routing code prune, _tags.md/_tag_links.md legacy headers); post-flip soak + gate record close
- Post-execution: gsd-verifier → 02-VERIFICATION.md; code review (Skill gsd-code-review "2", then --fix --auto if findings); then /gsd-autonomous for phases 3–4 and milestone lifecycle (audit → complete → cleanup)
</remaining_work>

<decisions_made>

- D-21..D-32 locked in 02-CONTEXT.md (triggerIndex inside _memory_catalog.json; engine choke-point rebuild; routability report from rebuild; one matcher over both levels; inline tuples; config-driven thresholds; hook retained as-is; mechanical no-LLM fallback; staged one-commit flip; MVR-exact legacy disposition + dead-code prune; probe harness drives the real hook)
- D-24 (skip FTS5) tagged `[informational]` — skip-decision honored by absence (gate format: tag inside the bold marker, `**D-24 [informational]:**`)
- Worktree isolation auto-degraded to sequential main-tree execution (#683)
- Executor model: sonnet; verifier: sonnet
</decisions_made>

<blockers>
- None. Working tree clean, no agents in flight.
</blockers>

## Required Reading (in order)
1. `.planning/phases/02-routing-index-live-recall-cutover/02-03-PLAN.md` — the next plan to execute (then 02-04)
2. `.planning/MVR.md` — the cutover gate; every item needs a real demonstration run before the 02-04 flip
3. `.planning/phases/02-routing-index-live-recall-cutover/02-CONTEXT.md` — locked decisions D-21..D-32
4. `.planning/phases/02-routing-index-live-recall-cutover/02-01-SUMMARY.md` + `02-02-SUMMARY.md` — what waves 1–2 actually shipped
5. `.planning/phases/02-routing-index-live-recall-cutover/02-RESEARCH.md` — perf budget analysis, probe pitfalls

## Critical Anti-Patterns (do NOT repeat these)
- PROBE-DEDUP-MASKING: probes/bench under per-run temp XDG_RUNTIME_DIR, never the live mark dir
- FLIP-WITHOUT-DEMONSTRATION: no MVR box gets checked by assertion or review — only by running the named demonstration command

## Infrastructure State
- Live hooks: ~/.claude/hooks/*.sh → synapse/hooks/*.sh symlinks (edits live immediately); kill-switch = `.surface-disabled` in the store
- Live store: ~/.claude/projects/-home-jangmanj/memory/ — catalog carries triggerIndex + routabilityReport (rebuilt by wave 1); legacy routing path still active and untouched; new matcher reachable only via `search-new` / MEMORY_SURFACE_SEARCH_IMPL=new
- Git: branch main, local commits NOT pushed (this is what degraded worktrees); working tree clean; no background agents or services

<context>
The autonomous workflow in use: per phase discuss → plan → execute, then code-review + fix, then verification routing; after all phases, audit → complete → cleanup. Phase 2's spine is the MVR gate. Budget watch for wave 3: research measured live full-hook p95 at 52–59ms against the ≤50ms gate; the in-process matcher is 0.84ms p95, so the overrun lives in the shell gate's jq calls — 02-03's jq-reduction lever is the identified fix; benchmark before and after.
</context>

<next_action>
Start with: `/gsd-execute-phase 2` — plan discovery skips 02-01/02-02 (SUMMARYs exist) and resumes at wave 3 (plan 02-03). After the phase verifies, run code review, then `/gsd-autonomous` for phases 3–4 and the lifecycle.
</next_action>
