---
phase: 04-reorganization-realignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/memory-review-offer.sh
  - memory/_review_game.py
  - tests/memory_surface/test_review_game.py
  - tests/memory_surface/run_shadow_validation.py
  - agent-harness.py
autonomous: true
requirements: [ORG-01, ORG-03]
must_haves:
  truths:
    - "No Roulette code exists in the repo or on the live box: hooks/memory-review-offer.sh, memory/_review_game.py, and tests/memory_surface/test_review_game.py are deleted, and the two live symlinks that pointed at them are removed BEFORE the sources (D-49, D-54 ordering)"
    - "~/.claude/hooks and the box-brain store contain zero dangling symlinks after the deletion"
    - "The install manifest is re-derived from the new core's needs: MEMORY_INFRA is exactly {\"_grammar.md\"} — _review_game.py deleted, _tags.md and _tag_links.md left in the store as unmanaged legacy symlinks that the harness never touches again (orchestrator resolution 1, principle 6: one routing vocabulary)"
    - "A real install dry-run → install --apply → status cycle confirms the post-deletion state: 12 hooks linked+registered, memory store assets list exactly _grammar.md, no memory-review-offer.sh anywhere (D-54 verified cycle)"
    - "memory/_grammar.md and memory/_tags.md are never staged, edited, moved, or reverted (D-56); the full pytest suite passes after the deletion (D-57)"
  artifacts:
    - path: "agent-harness.py"
      provides: "Re-derived install manifest"
      contains: "MEMORY_INFRA = {\"_grammar.md\"}"
  key_links:
    - from: "~/.claude/hooks/memory-review-offer.sh (live symlink)"
      to: "hooks/memory-review-offer.sh (source)"
      via: "rm of the symlink MUST precede deletion of the source (D-54 — Pitfall 1)"
      pattern: "memory-review-offer"
    - from: "agent-harness.py _memory_sources()"
      to: "MEMORY_INFRA set"
      via: "install/remove/status all iterate this set — manifest membership IS the install layout"
      pattern: "MEMORY_INFRA"
    - from: "store _tags.md / _tag_links.md symlinks"
      to: "lib/memory_surface.py validate"
      via: "validate still reads the store's _tags.md through the existing (now unmanaged) symlink — the symlinks must NOT be removed (Pitfall 6)"
      pattern: "_tags.md"
---

## Phase Goal

**As a** solo operator of this box's Claude Code harness, **I want to** strip the repo to exactly the components the post-flip core justifies — with the install layout re-derived from what that core actually needs — **so that** a fresh session or a fresh install is never misled by dead code, phantom manifest entries, or dangling symlinks.

(Recast as a user story from the ROADMAP Phase 4 Goal line; this is an infrastructure phase — each plan is a complete coherent slice: a deletion lands WITH its symlink/manifest/test updates in the same plan.)

This plan delivers the first slice: after it completes, Roulette's last physical remains are gone from both the repo and the live box, and `agent-harness.py install/remove/status` manage exactly the files the new core ships — nothing more.

<objective>
Execute the D-49 Roulette deletion with D-54 symlink-first ordering, then re-derive the install manifest: MEMORY_INFRA becomes {"_grammar.md"} per orchestrator resolution 1, and a real install/status cycle proves the clean state.

Purpose: ORG-01 (the store is a data directory — the code-in-store anomaly is removed) and ORG-03 (the install layout is re-derived from the new core's needs).
Output: deleted Roulette files, a clean live symlink topology, and an agent-harness.py whose manifest matches reality.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-reorganization-realignment/04-CONTEXT.md
@.planning/phases/04-reorganization-realignment/04-RESEARCH.md
</context>

## Artifacts this phase produces

Full phase inventory (this plan is 04-01; later plans reference back here):

- **04-01 (this plan):** Roulette deletion (`hooks/memory-review-offer.sh`, `memory/_review_game.py`, `memory/__pycache__/`, `tests/memory_surface/test_review_game.py` removed; live symlinks removed first); `agent-harness.py` with `MEMORY_INFRA = {"_grammar.md"}`; comment cleanup in `tests/memory_surface/run_shadow_validation.py`
- **04-02:** `lib/memory_surface.py` with the `_tag_links.md` write path excised (parse_tag_links/link/unlink/synonym_map/_drop_pair_lines gone, validate's links arm gone, CLI link/unlink arms gone) + D-51 dead-code sweep; lockstep case-pattern edits in `hooks/memory-write-guard.sh` + `hooks/memory-catalog-refresh.sh`; realigned `tests/memory_surface/test_phase1.py`, `test_phase2.py`, `test_write_hooks.sh`, `test_probe_runner.py`, `test_phase3.py`
- **04-03:** Realigned `README.md` (including the SC-1 component-justification table), `CLAUDE.md` (Architecture + Conventions sections), `CLAUDE.md.fragment`, `findings/memory-surfacing.md`; the D-55 verbatim install/status/remove demonstration record in the 04-03 SUMMARY

**Standing constraint for every task in every plan (D-56):** `memory/_grammar.md` and `memory/_tags.md` carry another session's uncommitted changes. Never edit, move, stage, commit, or revert them. Before every commit run `git status --porcelain memory/` — output must be exactly the two lines ` M memory/_grammar.md` and ` M memory/_tags.md`. Commit with explicit file paths only; never `git add -A`, `git add .`, or `git add memory/`.

<tasks>

<task type="auto">
  <name>Task 1: D-49 Roulette deletion — live symlinks first, then sources, then dead tests</name>
  <files>hooks/memory-review-offer.sh, memory/_review_game.py, tests/memory_surface/test_review_game.py, tests/memory_surface/run_shadow_validation.py</files>
  <read_first>
    - .planning/phases/04-reorganization-realignment/04-RESEARCH.md ("Dead-Code Inventory — D-49" and "Pattern: Symlink-Safe Deletion (D-54)" — the exact removal sequence and reverse-dependency list; "Common Pitfalls" 1, 4, 5)
    - agent-harness.py (lines 38–66 and 162–168: HOOKS_SRC glob + MEMORY_INFRA + _memory_sources() — why the harness can never clean up a symlink whose source is already gone)
    - tests/memory_surface/run_shadow_validation.py (line 29 — the comment-only _review_game.py reference to clean up)
  </read_first>
  <action>
D-54 ordering is the security-relevant part of this task: remove live symlinks BEFORE deleting their sources, because `agent-harness.py install`/`remove` iterate `HOOKS_SRC.glob("*.sh")` and `_memory_sources()` — once a source is gone, the harness can never see (and never remove) the stale live symlink (Pitfall 5).

Resolve the store path portably, never hardcoded (the `-home-jangman` hardcode is the documented prior failure): `KEY=$(printf '%s' "$HOME" | tr '/' '-')`, `STORE="$HOME/.claude/projects/$KEY/memory"`.

Step 1 — verify then remove the two live symlinks (per D-49):
- `ls -la "$HOME/.claude/hooks/memory-review-offer.sh"` (confirm it is a symlink into the lab), then `rm "$HOME/.claude/hooks/memory-review-offer.sh"`
- `ls -la "$STORE/_review_game.py"` (confirm symlink), then `rm "$STORE/_review_game.py"` — this is the store-side installation artifact the research flagged "DELETE after D-49"; nothing else will ever clean it

Step 2 — delete the sources from the repo:
- `git rm hooks/memory-review-offer.sh memory/_review_game.py tests/memory_surface/test_review_game.py` (explicit paths only — D-56)
- `rm -rf memory/__pycache__` (untracked bytecode from _review_game.py — Pitfall 4)

Step 3 — clean the comment-only reverse dependency: in tests/memory_surface/run_shadow_validation.py line 29, rewrite the comment `# Self-locate the engine using the _review_game.py pattern` to describe what it does without naming the deleted file (e.g. `# Self-locate the engine: sys.path.insert on <repo>/lib`). No logic change.

Step 4 — dangling-link audit: `find "$HOME/.claude/hooks" "$STORE" -maxdepth 1 -xtype l` must print nothing. Confirm the store's `_tags.md`, `_tag_links.md`, `_grammar.md` symlinks still exist and resolve (they are NOT touched by this task — Pitfall 6).

Do NOT touch memory/_grammar.md or memory/_tags.md (D-56). Do NOT delete memory/_tag_links.md — store content is data, not code (D-50).
  </action>
  <verify>
    <automated>KEY=$(printf '%s' "$HOME" | tr '/' '-'); STORE="$HOME/.claude/projects/$KEY/memory"; ! [ -e hooks/memory-review-offer.sh ] && ! [ -e memory/_review_game.py ] && ! [ -d memory/__pycache__ ] && ! [ -e tests/memory_surface/test_review_game.py ] && ! [ -L "$HOME/.claude/hooks/memory-review-offer.sh" ] && ! [ -L "$STORE/_review_game.py" ] && [ -z "$(find "$HOME/.claude/hooks" "$STORE" -maxdepth 1 -xtype l)" ] && [ -L "$STORE/_tags.md" ] && [ -L "$STORE/_tag_links.md" ] && ! grep -q '_review_game' tests/memory_surface/run_shadow_validation.py && python3 -m pytest tests/ -q && echo D49-CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Both live symlinks were removed BEFORE the source deletion (record the command order in the SUMMARY — D-54 discipline)
    - All four repo artifacts gone: hooks/memory-review-offer.sh, memory/_review_game.py, memory/__pycache__/, tests/memory_surface/test_review_game.py
    - `find "$HOME/.claude/hooks" "$STORE" -maxdepth 1 -xtype l` prints nothing (zero dangling links)
    - Store symlinks _tags.md, _tag_links.md, _grammar.md still present and resolving (untouched)
    - `grep -c '_review_game' tests/memory_surface/run_shadow_validation.py` prints 0
    - `python3 -m pytest tests/ -q` exits 0 with exactly 370 tests collected (383 minus the 13 deleted test_review_game.py tests — the delta must equal the enumerated deletion, nothing else lost)
    - `git status --porcelain memory/` shows exactly ` M memory/_grammar.md` and ` M memory/_tags.md` (D-56 untouched)
  </acceptance_criteria>
  <done>Roulette has no physical remains in the repo or on the live box, the deletion took its dead tests with it in the same commit, and the symlink topology is provably clean.</done>
</task>

<task type="auto">
  <name>Task 2: Install manifest re-derivation — MEMORY_INFRA = {"_grammar.md"} + verified install/status cycle</name>
  <files>agent-harness.py</files>
  <read_first>
    - agent-harness.py (lines 61–66: the GENERATED_MEMORY/MEMORY_INFRA block and its now-stale comment "Only taxonomy + game-engine infra is lab-sourced"; lines 134–159: link()/unlink_ours() semantics; lines 386–426: cmd_status output shape)
    - .planning/phases/04-reorganization-realignment/04-RESEARCH.md ("Install Manifest Audit" — current 3-entry state, collision analysis for _tags.md, and Pitfall 6)
    - .planning/phases/04-reorganization-realignment/04-CONTEXT.md (D-55 demonstration protocol — this task runs the same cycle as a wave gate; the full verbatim record happens in 04-03)
  </read_first>
  <action>
Re-derive the manifest from the new core's needs (ORG-03, orchestrator resolution 1 — baked in, no checkpoint):

Edit agent-harness.py line 65: `MEMORY_INFRA = {"_tags.md", "_tag_links.md", "_review_game.py"}` → `MEMORY_INFRA = {"_grammar.md"}`.

Membership rationale to encode in the replacement comment (rewrite lines 61–64): `_grammar.md` is the one lab-sourced routing-vocabulary artifact the rebuild consumes (principle 6: one routing vocabulary) and is install-managed; `_tags.md` and `_tag_links.md` remain in the store as inert legacy data files whose EXISTING symlinks the harness leaves in place but never manages, lists, or removes again; `_review_game.py` is deleted (D-49). The comment must not claim the harness manages _tags.md/_tag_links.md, and must note that removing the store's existing _tags.md symlink would break validate (Pitfall 6) — which is exactly why they are left alone rather than cleaned up.

Important pre-verified fact (do not "fix" it): the store's `_grammar.md` symlink already exists with target `../../../../JangLabs/synapse/memory/_grammar.md` — identical to what `_link_target()` computes — so `install` must report it "already linked", not replace it. If install reports anything else for _grammar.md, STOP and investigate before --apply.

Then run the D-54 verification cycle (real runs, output captured for the SUMMARY):
1. `./agent-harness.py install` (dry-run) — hooks section lists exactly 12 hooks, all "already linked"; memory store assets section lists ONLY `_grammar.md` ("already linked"); no `memory-review-offer.sh`, `_review_game.py`, `_tags.md`, or `_tag_links.md` anywhere in the plan
2. `./agent-harness.py install --apply` — completes with "Applied. Backups in .install-backups/<ts>"
3. `./agent-harness.py status` — 12 hooks all "linked" and "registered"; memory store assets shows only `_grammar.md: linked`; CLAUDE.md fragment present
4. Post-cycle invariants: `python3 lib/memory_surface.py validate` exits 0 (the store's unmanaged _tags.md symlink still feeds validate); config-drift-guard.sh and forbidden-files-guard.sh still registered (security invariant from RESEARCH "Security Domain")

The harness's no-permissions invariant is untouched — this change is set membership only.
  </action>
  <verify>
    <automated>grep -q 'MEMORY_INFRA = {"_grammar.md"}' agent-harness.py && ! grep -q '_review_game' agent-harness.py && ./agent-harness.py install | grep -q '_grammar.md' && ! ./agent-harness.py install | grep -qE 'review-offer|_review_game|_tag_links|_tags\.md' && ./agent-harness.py status | grep -c ': registered$' | grep -qx 12 && python3 lib/memory_surface.py validate && python3 -m pytest tests/ -q && echo ORG03-MANIFEST</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'MEMORY_INFRA = ' agent-harness.py` prints exactly `MEMORY_INFRA = {"_grammar.md"}` (one line)
    - `grep -c '_review_game' agent-harness.py` prints 0 and the rewritten manifest comment does not name `_review_game.py` as managed
    - install dry-run output: memory store assets section contains `_grammar.md` and contains none of `_tags.md`, `_tag_links.md`, `_review_game.py`, `memory-review-offer.sh`
    - `./agent-harness.py status`: all 12 hooks `linked` + `registered`; memory store assets lists only `_grammar.md: linked`
    - `python3 lib/memory_surface.py validate` exits 0 after the cycle (store vocabulary intact through the unmanaged symlink)
    - `./agent-harness.py status | grep -E 'config-drift-guard|forbidden-files-guard'` shows both registered (guards never deregistered)
    - Full pytest green (370); `git status --porcelain memory/` unchanged (D-56)
  </acceptance_criteria>
  <done>The install layout is re-derived from the new core: the harness manages 12 hooks + one grammar artifact + fragment + settings, a real apply/status cycle proves it, and the legacy store files are data the installer no longer claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source → live ~/.claude | Hooks and store assets are LIVE via symlinks; a source deletion without prior symlink removal leaves dangling links on a running box (D-54) |
| install --apply → live ~/.claude files | The harness rewrites live CLAUDE.md/settings.json/symlinks; manifest errors propagate to the running harness immediately |
| store data (D-56 files) → git index | Two store files carry another session's uncommitted work; any blanket staging command destroys or commits it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Denial of Service | live hook symlink topology | mitigate | D-54 ordering enforced as explicit numbered steps (symlink rm before source rm); `find -xtype l` zero-dangling gate in verify |
| T-04-02 | Tampering | store _tags.md symlink (validate dependency) | mitigate | Pitfall 6 pinned: manifest drops the entries but the existing store symlinks are explicitly NOT removed; `validate` exit-0 gate after the apply cycle |
| T-04-03 | Tampering | D-56 uncommitted store files | mitigate | Explicit-path `git rm`/`git add` only; `git status --porcelain memory/` two-line gate before every commit |
| T-04-04 | Elevation of Privilege | install --apply touching permissions | mitigate | Change is set-membership only; standing invariant (harness never writes `permissions`) unchanged; config-drift-guard + forbidden-files-guard registration asserted post-cycle |
| T-04-05 | Repudiation | "clean state" claimed but not shown | mitigate | install/status outputs captured into the SUMMARY; full verbatim D-55 record follows in 04-03 |
| T-04-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- D-54 sequence recorded: symlinks removed before sources
- `find "$HOME/.claude/hooks" "$STORE" -maxdepth 1 -xtype l` → empty
- `MEMORY_INFRA = {"_grammar.md"}`; install dry-run + apply + status outputs match the expected post-deletion shape (12 hooks, one memory asset)
- `python3 -m pytest tests/ -q` → 370 passed, exit 0 (wave gate, D-57)
- `bash tests/memory_surface/test_write_hooks.sh` → exit 0 (hooks untouched this plan; battery confirms it)
- `git status --porcelain memory/` → exactly the two pre-existing D-56 modifications
</verification>

<success_criteria>
- Roulette deletion complete with its dead tests and both live symlinks gone — no dangling links anywhere the harness manages (D-49, D-54)
- MEMORY_INFRA re-derived to {"_grammar.md"} with a comment that tells the truth about _tags.md/_tag_links.md being unmanaged legacy store data (ORG-03, orchestrator resolution 1)
- Real install/status cycle output proves the clean state; validate still works through the unmanaged store symlink
- 370/370 tests green; D-56 files untouched
</success_criteria>

<output>
Create `.planning/phases/04-reorganization-realignment/04-01-SUMMARY.md` when done. Record: the D-54 command order (verbatim), the install dry-run/apply/status outputs from Task 2, the pytest collected/passed counts before (383) and after (370), and the `git status --porcelain memory/` check result.
</output>
