---
phase: 01-trigger-grammar-write-time-intelligence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/MVR.md
  - memory/_grammar.md
  - tests/memory_surface/test_grammar.py
  - lib/memory_surface.py
autonomous: true
requirements: [MIG-01, CORE-01]
must_haves:
  truths:
    - ".planning/MVR.md is committed before any Phase-1 engine/hook code lands, and every checklist item names how it will be demonstrated"
    - "A grammar tag with zero behavioral evidence patterns (empty commands+paths+args) fails validate-grammar with exit 2"
    - "python3 lib/memory_surface.py validate-grammar exits 0 on the seeded memory/_grammar.md"
    - "memory/_grammar.md is reachable inside the box-brain store via a RELATIVE symlink, coexisting with legacy _tags.md/_tag_links.md"
    - "Legacy validate/rebuild/search behavior is byte-for-byte unchanged (old routing path untouched)"
  artifacts:
    - path: ".planning/MVR.md"
      provides: "Minimum Viable Replacement gate checklist (MIG-01)"
      contains: "MVR Gate"
    - path: "memory/_grammar.md"
      provides: "Unified trigger grammar v0 — spec header + 10-15 evidence-defined tag entries"
      contains: "placement:"
    - path: "tests/memory_surface/test_grammar.py"
      provides: "Spec-first contract tests for grammar parsing + schema validation"
      min_lines: 80
    - path: "lib/memory_surface.py"
      provides: "parse_grammar_md(), validate_grammar(), CLI validate-grammar subcommand"
      contains: "def parse_grammar_md"
  key_links:
    - from: "lib/memory_surface.py"
      to: "memory/_grammar.md"
      via: "parse_grammar_md(memdir / '_grammar.md')"
      pattern: "parse_grammar_md"
    - from: "lib/memory_surface.py main()"
      to: "validate_grammar"
      via: "CLI subcommand dispatch"
      pattern: "validate-grammar"
    - from: "~/.claude/projects/<key>/memory/_grammar.md"
      to: "memory/_grammar.md"
      via: "relative symlink ../../../../JangLabs/synapse/memory/_grammar.md"
---

<objective>
Deliver the two pre-code gates of Phase 1: the MVR gate checklist (MIG-01, D-16, D-17) committed FIRST, then the unified trigger grammar artifact `memory/_grammar.md` (CORE-01, D-01..D-06) — one machine-parseable source where every tag is defined by observable evidence patterns, schema-enforced so a tag without behavioral triggers cannot exist.

Purpose: MIG-01 requires the replacement gate to exist before any core implementation; CORE-01 collapses the three-file taxonomy (vocabulary + routing rules + tag links) into one grammar that Phase 2 compiles into the routing index.
Output: `.planning/MVR.md` (committed first), `memory/_grammar.md` v0 seeded with 10-15 evidence-defined tags + relative symlink into the box-brain store, spec-first contract tests, and engine support (`parse_grammar_md`, `validate_grammar`, `validate-grammar` CLI).
</objective>

## Phase Goal

**As a** Claude session writing a memory on this box, **I want to** save it with triggers derived in-context under one unified grammar, deduplicated against the store, and placed in the correct store by subject, **so that** the right memory surfaces at the right moment with zero human curation.

(Story assembled from the ROADMAP Phase 1 goal line + PROJECT.md core value — see planner note in plan-set summary.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-CONTEXT.md
@.planning/phases/01-trigger-grammar-write-time-intelligence/01-PATTERNS.md
</context>

<interfaces>
<!-- Key existing engine contracts the executor builds against. Extracted from lib/memory_surface.py. -->

From lib/memory_surface.py (read once; these are verified current):
- `TAG_RE = re.compile(r"^[a-z0-9][a-z0-9-]{1,39}$")` — tag-name shape, reuse for grammar tag names
- `FACET_HEADS = ("domain", "tool", "pattern")` — facet vocabulary, reuse for `## <facet>` headings
- `parse_tags_md(path) -> {"active", "deny", "overrides"}` — the re-based scanner model to extend (H2 sections, line regex, fail-open `if not path.exists()`)
- `_parse_flow_tags(value)` — `[a, b, c]` flow-array parsing; reuse its strip/split approach for grammar array fields
- `validate(memdir) -> list[str]` — error-list shape; `validate_grammar(memdir)` must return the same shape
- CLI: `main()` dispatches on `sys.argv[1]`; `validate` prints errors to stderr and returns 2; mirror this for `validate-grammar`
- `resolve_memdir()` honors `MEMORY_SURFACE_DIR` env override — all tests isolate via this
</interfaces>

## Artifacts this phase produces (this plan's share)

- File `.planning/MVR.md` — MVR gate checklist document
- File `memory/_grammar.md` — unified trigger grammar artifact (spec header + tag entries)
- File `tests/memory_surface/test_grammar.py` — contract test module
- Symlink `~/.claude/projects/<key>/memory/_grammar.md` → `../../../../JangLabs/synapse/memory/_grammar.md`
- Function `parse_grammar_md(path)` in `lib/memory_surface.py`
- Function `validate_grammar(memdir)` in `lib/memory_surface.py`
- Constant `PLACEMENTS = ("box", "project", "either")` in `lib/memory_surface.py`
- Constant `GRAMMAR_FIELDS = ("gloss", "placement", "commands", "paths", "args", "synonyms", "related")` in `lib/memory_surface.py`
- CLI subcommand `validate-grammar` in `lib/memory_surface.py main()`

<tasks>

<task type="auto">
  <name>Task 1: Write and commit .planning/MVR.md — the gate before any code (MIG-01, D-16, D-17)</name>
  <files>.planning/MVR.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 goal + 5 success criteria — the MVR items gate Phase 2's cutover)
    - .planning/phases/01-trigger-grammar-write-time-intelligence/01-RESEARCH.md — section "MVR Checklist Item Format" (the verified item template)
    - .planning/phases/01-trigger-grammar-write-time-intelligence/01-CONTEXT.md — decisions D-16, D-17
  </read_first>
  <action>
    Create `.planning/MVR.md` per D-16/D-17. Structure: title "MVR Gate: Minimum Viable Replacement Checklist", a status banner `> **Status:** OPEN (Phase 2 may remove the old routing path only when ALL items are CHECKED)`, then exactly these 8 checklist items (each `- [ ]`, each naming its demonstration method — demonstrable, never asserted):
    1. All ~140 existing memories routable under the new system — bulk trigger derivation complete OR tag-only fallback confirmed working for every store memory (demonstrated by: rebuild output listing 0 unroutable memories).
    2. Reference probes pass both directions — at least 5 obvious-should-fire synthetic payloads fire with their evidence tuple; at least 5 obvious-should-stay-silent payloads stay silent (demonstrated by: probe script run output).
    3. Per-tool-call recall adds ≤ 50ms p95 wall time on this box (demonstrated by: time.perf_counter() measurement, minimum 20 samples; baseline 28-51ms).
    4. Every recall block cites its evidence tuple {tag, trigger_type, matched_value} (demonstrated by: probe output inspection).
    5. One command rebuilds the routing index fully from store contents from a cold state (demonstrated by: delete catalog, run `python3 lib/memory_surface.py rebuild`, verify routing works).
    6. Fail-open verified — with `.surface-disabled` present, all memory hooks exit 0 with no output (demonstrated by: sample-JSON stdin runs).
    7. Kill-switch / infra-fault verified — with `_memory_catalog.json` missing, recall hook exits 0, never rc 2 (demonstrated by: sample-JSON stdin runs).
    8. Old-path removal steps enumerated — exact ordered list of what gets removed/disabled (`memory-recall.sh` routing behavior, `_tags.md`/`_tag_links.md` retirement), with a verification step per item.
    Close with a note: "This gate is OPEN through Phase 1. The legacy routing path stays live and untouched until every box is checked (MIG-01)."
    Commit this file IMMEDIATELY as this phase's first commit, message `docs(01-01): add MVR gate checklist (MIG-01)` — before any other Phase-1 task touches code (D-16: all core code tasks depend on this commit existing).
  </action>
  <verify>
    <automated>test -f .planning/MVR.md && [ "$(grep -c '^- \[ \]' .planning/MVR.md)" -eq 8 ] && git log --oneline -1 -- .planning/MVR.md | grep -q "MIG-01"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/MVR.md` exists; `grep -c '^- \[ \]' .planning/MVR.md` outputs 8
    - `grep -F '{tag, trigger_type, matched_value}' .planning/MVR.md` matches (evidence-tuple item present)
    - `grep -F '50ms' .planning/MVR.md` matches (latency item present)
    - `grep -F '.surface-disabled' .planning/MVR.md` matches (fail-open item present)
    - `git log --oneline -- .planning/MVR.md` shows a commit, and that commit predates every other Phase-1 code commit in this plan set
  </acceptance_criteria>
  <done>MVR.md committed as the first Phase-1 commit; 8 demonstrable gate items present; status OPEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author memory/_grammar.md v0 + spec-first contract tests (RED) (CORE-01, D-02, D-03, D-05, D-06)</name>
  <files>memory/_grammar.md, tests/memory_surface/test_grammar.py</files>
  <read_first>
    - memory/_tags.md (legacy vocabulary — D-05 seed input; domain + tool facets only)
    - memory/_tag_links.md (Path Tags section — primary evidence-pattern seed; Synonyms section — synonym seed)
    - .planning/phases/01-trigger-grammar-write-time-intelligence/01-PATTERNS.md — sections "memory/_grammar.md" and "tests/memory_surface/test_phase1_grammar.py" (entry layout + test file structure)
    - tests/memory_surface/test_phase1.py (existing fixture/test conventions: module-level fixture strings, TempStore setUp/tearDown, MEMORY_SURFACE_DIR isolation, stdlib unittest only)
  </read_first>
  <behavior>
    - Test (D-03): a tag entry with at least one entry across commands/paths/args parses and passes validate_grammar
    - Test (D-03): a tag entry with empty commands, paths AND args (synonyms-only) produces a validate_grammar error naming the tag — "a tag without observable triggers cannot exist"
    - Test (D-03): placement value outside {box, project, either} produces an error; missing placement defaults to "either"
    - Test (D-03): a `related:` entry referencing an undefined tag produces an error
    - Test (D-02): parse_grammar_md returns {} for a missing file (fail-open, same as parse_tags_md)
    - Test (D-02): array fields parse both `[a, b]` flow form and bare `a, b` form; quotes stripped
    - Test: tag names violating ^[a-z0-9][a-z0-9-]{1,39}$ produce an error
    - Test (D-04): parsed entry dict uses EXACTLY the field names commands/paths/args/synonyms (the same vocabulary per-memory triggers: will use — pin the names)
  </behavior>
  <action>
    Two artifacts, spec FIRST, tests FROM the spec (D-06 ordering; the "111 green tests, 13 dead rules" pitfall means tests assert the spec document text, never implementation behavior).

    (1) `memory/_grammar.md` — the unified grammar artifact AND its own spec document. Header section (markdown prose before the first `##` facet heading) states the normative grammar rules: `## <facet>` headings (facet from domain|tool|pattern); `### <tag>` starts an entry (tag matches ^[a-z0-9][a-z0-9-]{1,39}$); field lines `gloss:`, `placement:`, `commands:`, `paths:`, `args:`, `synonyms:`, `related:`; array fields use `[a, b, c]` flow form; SCHEMA RULE (D-03): every tag MUST declare at least one behavioral evidence pattern across commands+paths+args — synonyms alone do not qualify; placement MUST be one of box|project|either (default either); related entries MUST reference tags defined in this file. State explicitly (D-04): per-memory `metadata.triggers:` blocks use the SAME field names (commands/paths/args/synonyms) with the SAME matching semantics — one grammar, one future matcher.

    Seed entries (D-05 — port ONLY evidence-definable tags; pattern-facet lesson tags from _tags.md are NOT ported). Recommended seed cut, audited from _tag_links.md Path Tags + _tags.md domain/tool facets (executor adjusts per-tag against "≥1 real behavioral evidence pattern", keeping 10-15 entries; glosses come from _tags.md):
    - domain: nvidia (commands: nvidia-smi, supergfxctl, modinfo; synonyms: nvidia-open; placement: box), boot (commands: limine, limine-mkinitcpio, mkinitcpio, bootctl; paths: /efi/**, /boot/**; placement: box), kde-plasma (commands: kwriteconfig6, kreadconfig6, plasmashell; paths: ~/.config/kwinrc, ~/.config/plasma**; placement: box), shell (commands: fish, zsh, chsh; paths: ~/.zshenv, ~/.config/fish/**; placement: box), terminal (commands: kitty, ghostty; paths: ~/.config/kitty/**, ~/.config/ghostty/**; placement: box), remote-access (commands: rustdesk, tailscale; synonyms: remote-desktop, rdp; placement: box), asus-rog (commands: asusctl, supergfxctl; placement: box), claude-harness (commands: claude; paths: ~/.claude/**; synonyms: claude-code; placement: box), audio (commands: wpctl, pw-record, amixer; paths: ~/.config/pipewire/**; placement: box), cachyos-kernel (commands: scx_loader; synonyms: sched-ext; placement: box)
    - tool: systemd (commands: systemctl, journalctl, systemd-run; placement: box), pacman (commands: pacman, paru, makepkg; placement: box), limine (commands: limine, limine-mkinitcpio; related: boot; placement: box), pipewire (commands: wpctl, pw-record; related: audio; placement: box), git (commands: git; placement: either)
    Use `related:` for the co-trigger pairs visible in _tag_links.md rules (e.g. limine↔boot, supergfxctl rules → asus-rog↔nvidia, pipewire↔audio) — this replaces the separate _tag_links.md graph per D-03. Do NOT modify _tags.md or _tag_links.md (D-01: coexistence).

    (2) `tests/memory_surface/test_grammar.py` — stdlib unittest, modeled on tests/memory_surface/test_phase1.py (LAB = parents[2]; sys.path.insert lib; module-level GRAMMAR_MD fixture strings; TempStore base class with tempfile store). Every test docstring cites the decision (D-02/D-03/D-04) or the _grammar.md spec-header sentence it pins. Cover the behaviors listed in <behavior>. Run it now: tests MUST fail (AttributeError: parse_grammar_md does not exist yet) — RED is the required end state of this task. Do NOT implement engine code in this task.
  </action>
  <verify>
    <automated>test -f memory/_grammar.md && test -f tests/memory_surface/test_grammar.py && { python3 tests/memory_surface/test_grammar.py 2>&1 | grep -qE "(FAILED|Error)" ; }</automated>
  </verify>
  <acceptance_criteria>
    - `memory/_grammar.md` exists with a spec header stating the D-03 schema rule, and 10-15 `### <tag>` entries; `grep -c '^### ' memory/_grammar.md` outputs a value in 10..15
    - Every entry has a non-empty gloss and a placement line; `grep -v '^#' memory/_grammar.md | grep -c '^placement:'` equals the `### ` count
    - No pattern-facet lesson tag from _tags.md (e.g. verify-live, self-kill-trap) appears as a `### ` entry (D-05)
    - `memory/_tags.md` and `memory/_tag_links.md` are unmodified (`git diff --name-only` does not list them)
    - `python3 tests/memory_surface/test_grammar.py` FAILS (RED) with errors referencing parse_grammar_md/validate_grammar — proof tests were written before code
    - Test docstrings cite D-02/D-03/D-04 or spec-header text, not implementation details
  </acceptance_criteria>
  <done>Grammar v0 artifact exists with schema rules stated in-file; contract tests written from that spec and failing RED</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement parse_grammar_md + validate_grammar + CLI subcommand; symlink into store (GREEN) (CORE-01, D-01, D-02, D-06)</name>
  <files>lib/memory_surface.py</files>
  <read_first>
    - lib/memory_surface.py (current state — especially parse_tags_md, _parse_flow_tags, validate, main(), VALUE_FLAGS; replicate these exact conventions)
    - tests/memory_surface/test_grammar.py (the contract you are implementing against — written in Task 2)
  </read_first>
  <behavior>
    - All test_grammar.py tests pass (GREEN)
    - Full existing suite stays green (no legacy behavior change)
  </behavior>
  <action>
    Implement in `lib/memory_surface.py`, extending the proven parse_tags_md scanner pattern (D-02 — stdlib re only, no PyYAML, no JSON for this artifact):
    - Module constants: `PLACEMENTS = ("box", "project", "either")`, `GRAMMAR_FIELDS = ("gloss", "placement", "commands", "paths", "args", "synonyms", "related")`.
    - `parse_grammar_md(path)` → `{tag: {"facet", "gloss", "placement", "commands", "paths", "args", "synonyms", "related"}}`. Scanner: `## <facet>` sets active facet; `### <tag>` opens an entry initialized with empty lists, placement "either", gloss ""; `<field>: <value>` lines fill fields — array fields parsed via the `_parse_flow_tags` strip-brackets/split-comma/strip-quotes approach; unknown field names are recorded for validation (do not crash). `if not path.exists(): return {}` (fail open, matching every existing parser).
    - `validate_grammar(memdir)` → error-string list (same shape as validate()): tag name fails TAG_RE; gloss empty; placement not in PLACEMENTS; ZERO behavioral evidence (len(commands)+len(paths)+len(args) == 0 — the D-03 schema-enforcement rule; synonyms alone do not qualify); related references an undefined tag; unknown field name; facet not in FACET_HEADS.
    - CLI: add `validate-grammar` arm to main() mirroring the `validate` arm exactly (print errors to stderr, return 0 if clean else 2). Do NOT change the legacy `validate` subcommand (the old path stays live — Phase constraint).
    - Create the store symlink (D-01, relative, same pattern as _tags.md): run from the live store directory `cd ~/.claude/projects/$(printf '%s' "$HOME" | tr '/' '-')/memory/ && ln -sfn "../../../../JangLabs/synapse/memory/_grammar.md" _grammar.md`. Never an absolute symlink.
    - Confirm zero legacy impact: `_grammar.md` is underscore-prefixed so `_memory_files()` skips it, `fingerprint()` ignores it, and the live guard's `MEMORY.md|_*` arm exempts it — no rebuild/search behavior changes.
  </action>
  <verify>
    <automated>python3 tests/memory_surface/test_grammar.py && python3 -m unittest discover -s tests/memory_surface -p 'test_*.py' && python3 lib/memory_surface.py validate-grammar</automated>
  </verify>
  <acceptance_criteria>
    - `python3 tests/memory_surface/test_grammar.py` exits 0 (GREEN)
    - `python3 -m unittest discover -s tests/memory_surface -p 'test_*.py'` exits 0 (legacy suites untouched and green)
    - `python3 lib/memory_surface.py validate-grammar` exits 0 against the live store (seeded grammar valid)
    - A synthetic store whose _grammar.md has a synonyms-only tag makes `MEMORY_SURFACE_DIR=<tmp> python3 lib/memory_surface.py validate-grammar` exit 2 with the tag named on stderr
    - `readlink "$HOME/.claude/projects/$(printf '%s' "$HOME" | tr '/' '-')/memory/_grammar.md"` outputs exactly `../../../../JangLabs/synapse/memory/_grammar.md`
    - `git diff lib/memory_surface.py` contains no edits to validate(), rebuild(), search(), extract_tokens() bodies
  </acceptance_criteria>
  <done>Grammar parser + schema validator live in the engine, contract tests green, full suite green, grammar symlinked into the store alongside the untouched legacy taxonomy</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| _grammar.md content → engine parser | Human/model-curated markdown parsed by re scanner; treated as untrusted input |
| CLI argv/stdin → engine | validate-grammar runs in hook context later; must never block on infra faults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | parse_grammar_md | mitigate | Pattern-match only — no eval, no re.DOTALL over file-supplied regex; tag names gated by TAG_RE; unknown fields surface as validation errors, never executed (ASVS V5 input validation) |
| T-01-02 | Denial of Service | validate-grammar in future guard path | mitigate | Missing _grammar.md returns {} (fail open, rc 0); only genuine schema violations return rc 2 with non-empty reason — preserves the existing infra-fault fail-open invariant |
| T-01-03 | Tampering | store symlink | mitigate | Relative symlink only (absolute breaks on clone/rename — established repo rule); created with ln -sfn from the store dir |
| T-01-SC | Tampering | package installs | accept | No package installs this phase — stack is 100% stdlib + jq already present (RESEARCH.md Package Legitimacy Audit is vacuous) |
</threat_model>

<verification>
- `git log --oneline -- .planning/MVR.md` shows the MVR commit preceding all engine-code commits of this plan
- `python3 -m unittest discover -s tests/memory_surface -p 'test_*.py'` green
- `python3 lib/memory_surface.py validate-grammar` rc 0; broken-fixture store rc 2
- `git status` clean for memory/_tags.md and memory/_tag_links.md (legacy untouched)
</verification>

<success_criteria>
- MIG-01 satisfied: MVR gate checklist committed before any core code, 8 demonstrable items
- CORE-01 satisfied at artifact level: one unified grammar defines tags by evidence patterns; schema validation rejects evidence-less tags; legacy taxonomy coexists untouched
- Spec-first discipline (D-19) proven: test file commit/RED run precedes implementation
</success_criteria>

<output>
Create `.planning/phases/01-trigger-grammar-write-time-intelligence/01-01-SUMMARY.md` when done. Record: the exact seed-tag list that made the D-05 cut (and which legacy tags were excluded and why), the final grammar field grammar (for plans 02-04 to reference), and the validate-grammar CLI contract.
</output>
