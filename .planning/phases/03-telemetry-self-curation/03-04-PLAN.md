---
phase: 03-telemetry-self-curation
plan: 04
type: execute
wave: 4
depends_on: [03-02, 03-03]
files_modified:
  - lib/memory_surface.py
  - tests/memory_surface/seat_probes.py
  - tests/memory_surface/test_phase3.py
autonomous: true
requirements: [CUR-05]
must_haves:
  truths:
    - "A rerunnable probe runner drives each MEMORY.md router seat's derived payload through the REAL memory-recall.sh and records per-seat whether live recall covers it — written to _seat_probe_results.json (D-47 condition a, real run)"
    - "The seats engine subcommand proposes a seat DEMOTION only when BOTH the probe result shows coverage AND telemetry shows real fires for that memory, AND the evidence window (≥10 session records or ≥30 days of telemetry span) is met — otherwise it refuses with a stated reason (D-47 + resolved discretion)"
    - "Seat changes are applied as a PENDING-SEAT-CHANGES comment block at the top of MEMORY.md — visible, vetoable, idempotent, never a silent edit; deleting the block is the human approval action (D-48; box-brain store verified NOT git-tracked)"
    - "The live demonstration is honest: the probe run's real per-seat results and the governance run's evidence-window refusal are committed as a phase artifact; the live MEMORY.md is byte-unchanged by the refusing run"
    - "Seat governance runs with the maintenance pass (same telemetry, same D-40 trigger) — machine-decided, no hand-audit (CUR-05)"
  artifacts:
    - path: "tests/memory_surface/seat_probes.py"
      provides: "Per-seat probe runner through the real hook; writes _seat_probe_results.json"
      contains: "_seat_probe_results"
    - path: "lib/memory_surface.py"
      provides: "seats(memdir) + _parse_seat_stems() + _write_pending_block(); CLI subcommand seats; wired into maintenance()"
      contains: "def seats"
    - path: ".planning/phases/03-telemetry-self-curation/03-SEAT-GOVERNANCE.md"
      provides: "Committed real probe table (11 seats), live refusal output, fixture end-to-end pending-block demonstration"
      contains: "PENDING-SEAT-CHANGES"
  key_links:
    - from: "tests/memory_surface/seat_probes.py"
      to: "hooks/memory-recall.sh"
      via: "subprocess bash invocation, fixed argv, MEMORY_SURFACE_DIR + per-run temp XDG_RUNTIME_DIR (the test_probe_runner.py pattern)"
      pattern: "memory-recall.sh"
    - from: "lib/memory_surface.py seats()"
      to: "$STORE/_seat_probe_results.json"
      via: "sidecar read — absent file means condition (a) unmet for all seats (fail-safe: no demotions)"
      pattern: "_seat_probe_results"
    - from: "lib/memory_surface.py seats()"
      to: "$STORE/MEMORY.md"
      via: "targeted idempotent PENDING-SEAT-CHANGES block replace via write_atomic — never through _memory_files()"
      pattern: "PENDING-SEAT-CHANGES"
---

## Phase Goal

**As a** solo operator of this box's memory harness, **I want to** have every recall fire logged, every read-confirmation detected, and curation decided automatically from that evidence, **so that** the right memories keep surfacing with zero human curation ritual — Roulette retired and router seats machine-governed.

This plan delivers the final vertical slice: after it completes, the MEMORY.md router's 11 seats are under machine governance — probe-proven, telemetry-gated, with proposed changes surfacing as a visible, vetoable pending block instead of a hand-audit (CUR-05).

<objective>
Put base-floor seat membership under machine governance: a probe runner that proves per-seat live-recall coverage through the real hook (D-47 condition a), a `seats` engine subcommand that combines probe proof + real-fire telemetry + an evidence-window gate into demote/promote proposals, and the D-48 pending-change block as the application mechanism — demonstrated with real live runs and a fixture end-to-end.

Purpose: CUR-05 — seat changes machine-decided, demotion only once probes prove recall covers the seat, visible and vetoable without hand-audit.
Output: seat_probes.py, the seats subcommand wired into maintenance, SeatGovernance contract tests, and the committed 03-SEAT-GOVERNANCE.md demonstration artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-telemetry-self-curation/03-CONTEXT.md
@.planning/phases/03-telemetry-self-curation/03-RESEARCH.md
@.planning/phases/03-telemetry-self-curation/03-PATTERNS.md
@.planning/phases/03-telemetry-self-curation/03-02-SUMMARY.md
@.planning/phases/03-telemetry-self-curation/03-03-SUMMARY.md
</context>

## Artifacts this phase produces

Full phase inventory lives in 03-01-PLAN.md. This plan creates:

- `tests/memory_surface/seat_probes.py` — `--store` arg (default live); parses seat stems from MEMORY.md; derives per-seat probe payloads from frontmatter triggers; drives the REAL memory-recall.sh; writes `<store>/_seat_probe_results.json` (`{generatedTs, results: {stem: {covered, payload, matched}}}`)
- `lib/memory_surface.py` — `seats(memdir)`, `_parse_seat_stems(memdir)`, `_write_pending_block(memdir, proposals)`; CLI subcommand `seats`; call wired into the end of non-shadow `maintenance()`; optional config key `seatPromoteMinFires` (default 5) via `cfg.get`
- `$STORE/_seat_probe_results.json` — probe-coverage sidecar (created by the runner)
- `.planning/phases/03-telemetry-self-curation/03-SEAT-GOVERNANCE.md` — committed demonstration artifact
- `tests/memory_surface/test_phase3.py` — `SeatGovernance` class

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Seat probe runner through the real hook (D-47 condition a)</name>
  <files>tests/memory_surface/seat_probes.py, tests/memory_surface/test_phase3.py</files>
  <read_first>
    - tests/memory_surface/test_probe_runner.py (run_hook subprocess shape — fixed argv, shell=False, MEMORY_SURFACE_DIR + per-run temp XDG_RUNTIME_DIR, clear_dedup_marks via Path.glob/unlink; reuse, do not reinvent)
    - The live MEMORY.md at ~/.claude/projects/-home-jangmanj/memory/MEMORY.md (seat-line format: markdown links `- [Title](stem.md) — …` under the `## Always-relevant entries` heading; note the seat bar in its prose — a seat exists precisely because recall could NOT cover it, so covered:false results are expected and meaningful)
    - .planning/phases/03-telemetry-self-curation/03-RESEARCH.md (Q5 — the 11 current seats; D-47 proof standard: real hook invocation, not assertion)
    - lib/memory_surface.py (parse_frontmatter — to read each seat memory's triggers: block for payload derivation; write_atomic — for the results sidecar)
  </read_first>
  <behavior>
    SeatGovernance (probe half), fixture stores:
    - Fixture MEMORY.md with two seat links + matching memories, one having a command trigger that fires → its result is covered:true with the matched payload recorded
    - Seat memory with NO triggers: block → covered:false with reason "no-derivable-probe" (condition (a) unmet, fail-safe)
    - Seat link naming a non-existent file → recorded as covered:false with reason "missing-memory", runner does not crash
    - _seat_probe_results.json is valid JSON with generatedTs and one results entry per seat stem; runner exits 0
  </behavior>
  <action>
Tests first (RED), then the runner (GREEN).

`tests/memory_surface/seat_probes.py` (stdlib only): parse seat stems from the target store's MEMORY.md by scanning the `## Always-relevant entries` section for markdown-link targets matching `*.md` basenames (regex on the link target, store-relative only — no paths with slashes). For each seat stem: read the memory file's frontmatter via the engine's parse_frontmatter (sys.path.insert lib); derive ONE probe payload from its triggers: block — first command trigger → `{"tool_name":"Bash","tool_input":{"command":"<command> --help"},"cwd":"/tmp"}` shaped payload using the bare command; else first path trigger → `{"tool_name":"Read","tool_input":{"file_path":"<concrete path instantiated from the glob>"},"cwd":"/tmp"}`; no triggers → record covered:false / "no-derivable-probe" without invoking the hook. Invoke the REAL hooks/memory-recall.sh exactly as test_probe_runner.py does: subprocess with fixed argv, payload on stdin, env carrying MEMORY_SURFACE_DIR=<store> and a per-run temp XDG_RUNTIME_DIR (probe marks must never touch the live session's dedup state), timeout 5. covered:true iff exit 0, stdout parses as the hook emission JSON, and the additionalContext block names the seat's stem among its numbered entries. Clear marks between seats (marks accumulate within the run — the research Pitfall-4 class). Write `<store>/_seat_probe_results.json` atomically (tmp + os.replace) with generatedTs (ISO UTC) and per-stem `{covered, payload, matched, reason?}`. Exit 0 regardless of coverage outcomes — the file reports; governance judges.
  </action>
  <verify>
    <automated>python3 tests/memory_surface/test_phase3.py && python3 tests/memory_surface/seat_probes.py --store "$(mktemp -d)" 2>/dev/null; python3 tests/memory_surface/test_probe_runner.py</automated>
  </verify>
  <acceptance_criteria>
    - All probe-half behaviors pass; empty/absent MEMORY.md store handled silently (exit 0, empty results)
    - `grep -c "shell=True" tests/memory_surface/seat_probes.py` prints 0; env override for XDG_RUNTIME_DIR grep-visible
    - Existing probe suite still green (shared infrastructure untouched or safely reused)
  </acceptance_criteria>
  <done>Any store's router seats can be probe-tested through the real recall hook with isolated dedup state, producing a machine-readable coverage sidecar — the D-47(a) instrument.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: seats engine subcommand + pending-change block + maintenance wiring (D-47 gate, D-48)</name>
  <files>lib/memory_surface.py, tests/memory_surface/test_phase3.py</files>
  <read_first>
    - lib/memory_surface.py (maintenance() as shipped by 03-02 — the wiring point; _read_telemetry — extend or add a sibling helper for session counts and ts span; write_atomic; load_config; CLI dispatch)
    - .planning/phases/03-telemetry-self-curation/03-RESEARCH.md (Q4 — pending-change block format suggestion; Q5 — seat list + WAVE-2 timing constraint; Open Question 1 — the ≥10 sessions / ≥30 days window recommendation, adopted here as the planner-resolved discretion)
    - .planning/phases/03-telemetry-self-curation/03-CONTEXT.md (D-47 exact dual-condition wording; D-48 pending-block path — box-brain verified NOT git-tracked, no git commit path)
    - hooks/memory-base-floor.sh (the {ts, signal:"session"} marker from 03-02 — the session-count evidence source)
  </read_first>
  <behavior>
    SeatGovernance (governance half), fixture stores with synthetic telemetry and probe sidecars:
    - No _seat_probe_results.json in the store → zero demote proposals regardless of telemetry (fail-safe; reason "no-probe-results" in the result)
    - Seat covered:true + fired in telemetry, but window unmet (3 session records, 5-day span) → refused; result names the unmet window with its numbers
    - Window met (≥10 session records or ≥30-day telemetry span) + covered:true + fire_count ≥ 1 → DEMOTE proposal; PENDING-SEAT-CHANGES comment block lands at the top of fixture MEMORY.md with the stem, fire/read numbers, and probe reference
    - Non-seat memory with fire_count ≥ seatPromoteMinFires (default 5) and read_rate ≥ promoteThreshold → PROMOTE proposal line with its numbers
    - Re-running seats replaces the existing pending block (one block, no stacking); the rest of MEMORY.md is byte-identical before/after (router content never truncated or rewritten)
    - No proposals → no pending block written; a stale pending block from a prior run is removed
    - maintenance() non-shadow invokes seats(); maintenance-shadow does NOT write any pending block
  </behavior>
  <action>
Tests first (RED), then implement (GREEN).

`_parse_seat_stems(memdir)`: same parsing rule as seat_probes.py (markdown-link .md targets under the Always-relevant heading) — keep the two implementations trivially small and pinned by a shared fixture so they cannot drift. `seats(memdir)`: load telemetry via _read_telemetry plus a window-stats helper returning session-record count (signal:"session" lines) and overall telemetry ts span in days; evidence window = sessions ≥ 10 OR span ≥ 30 days (planner-resolved discretion per RESEARCH Open Question 1; comment that SessionStart re-fires on resume/clear/compact make the session count an upper-bound proxy, and that the pending block's human veto is the safety net). DEMOTE candidates: seat stems where probe sidecar says covered:true AND windowed fire_count ≥ 1 AND window met — per D-47, coverage proof means the always-on seat is redundant with recall. PROMOTE candidates: non-seat memory stems with windowed fire_count ≥ cfg.get("seatPromoteMinFires", 5) AND read_rate ≥ promoteThreshold. Each proposal line carries its evidence numbers (fires, reads, rate, probe payload for demotes) in the RESEARCH Q4 format.

`_write_pending_block(memdir, proposals)`: targeted edit of MEMORY.md ONLY — read the file, strip any existing block delimited by `<!-- PENDING-SEAT-CHANGES` … `-->`, prepend the new block (or nothing when proposals are empty), write_atomic. Never reached via _memory_files() (MEMORY.md is excluded there by design — this is the one sanctioned MEMORY.md writer and it must stay surgical). Block text: header `PENDING-SEAT-CHANGES (automated, <ISO date>) — review and delete this block to approve:` followed by one DEMOTE/PROMOTE line per proposal.

CLI: `seats` subcommand printing a one-line result (`seats: N demote, M promote proposals` or `seats: window unmet (X sessions, Y days)` or `seats: no probe results`), exit 0 always, exception-proof like maintenance. Wiring: call seats(memdir) at the end of maintenance() when not shadow — same telemetry, same D-40 SessionStart cadence satisfies CUR-05's "governed by the same telemetry" with no new trigger surface. Engine regression suites must stay green.
  </action>
  <verify>
    <automated>python3 tests/memory_surface/test_phase3.py && python3 tests/memory_surface/test_routing_contract.py && python3 lib/memory_surface.py seats | grep -E "^seats:"</automated>
  </verify>
  <acceptance_criteria>
    - All governance-half behaviors pass, including block idempotency and the byte-identical-router test
    - `python3 lib/memory_surface.py seats` against the live store exits 0 and prints a single seats: line (live smoke)
    - The dual D-47 gate is structural: demote requires probe-covered AND fired AND window — each independently refusable, each refusal visible in the result
    - maintenance-shadow provably writes no pending block (test-pinned)
    - test_routing_contract.py green (engine regression-free)
  </acceptance_criteria>
  <done>Seat membership is machine-decided from probe proof + telemetry behind an evidence window, applied only as a visible, vetoable, idempotent pending block — never a silent router edit.</done>
</task>

<task type="auto">
  <name>Task 3: Real demonstrations — live probe table, live refusal, fixture end-to-end (D-47 proof + phase exit checks)</name>
  <files>tests/memory_surface/test_phase3.py</files>
  <read_first>
    - tests/memory_surface/seat_probes.py and lib/memory_surface.py seats() (as shipped by Tasks 1–2)
    - .planning/phases/03-telemetry-self-curation/03-RESEARCH.md (Q5 — the 11 live seats by name; the timing note: telemetry-side seat decisions need accumulated real sessions, so refusal IS the correct live outcome now)
  </read_first>
  <action>
Live probe run (the D-47(a) REAL demonstration — no box checked by assertion): `python3 tests/memory_surface/seat_probes.py` against the live store. Record the full per-seat table (11 seats × covered/reason/payload) in a new committed artifact `.planning/phases/03-telemetry-self-curation/03-SEAT-GOVERNANCE.md`. Interpret honestly: the router's own seat bar says a seat exists because recall could NOT cover it — covered:false rows confirm seats are earning their place; any covered:true row is a genuine future demotion candidate once telemetry accrues.

Live governance run: checksum the live MEMORY.md (sha256sum), run `python3 lib/memory_surface.py seats`, record its refusal line (expected: window unmet — telemetry began this phase), re-checksum and record that MEMORY.md is byte-identical. This is the honest live outcome: governance is armed, evidence-gated, and refused correctly.

Fixture end-to-end: fixture store with one seat memory (command trigger), synthetic telemetry containing ≥10 session records + in-window fires for the seat + a high-read non-seat memory, probe sidecar from a real seat_probes.py run against the fixture → run `python3 lib/memory_surface.py maintenance` (non-shadow) and show the PENDING-SEAT-CHANGES block landing in the fixture MEMORY.md with one DEMOTE and one PROMOTE line. Paste the block verbatim into the artifact.

Close the artifact with the standing state: governance re-runs automatically with every maintenance pass; the first live seat decision is expected once ≥10 sessions or ≥30 days of telemetry exist; approval = human deletes the block (D-48, store not git-tracked).

Phase exit checks: full memory_surface suite green (`for f in tests/memory_surface/test_*.py; do python3 "$f"; done` equivalent), probe suite both modes, and a final `bash tests/memory_surface/bench_recall.sh -n 20` — the phase ships only with the recall budget intact end to end.
  </action>
  <verify>
    <automated>python3 tests/memory_surface/test_phase3.py && python3 tests/memory_surface/test_probe_runner.py && PROBE_LIVE=1 python3 tests/memory_surface/test_probe_runner.py && bash tests/memory_surface/bench_recall.sh -n 20 | grep -E "^p95_ms=" && grep -q "PENDING-SEAT-CHANGES" .planning/phases/03-telemetry-self-curation/03-SEAT-GOVERNANCE.md && echo D47-RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - 03-SEAT-GOVERNANCE.md committed with: the full 11-seat live probe table, the live refusal line, the before/after MEMORY.md checksums (identical), and the verbatim fixture pending block
    - Live MEMORY.md byte-unchanged by the live governance run
    - Full test suite + probe suite (both modes) green; final p95_ms ≤ 55 recorded
  </acceptance_criteria>
  <done>D-47 is demonstrated with real runs — live probe coverage per seat, a correct live evidence refusal, and a fixture proof of the full demote/promote pending-block path — and the phase exits with the recall budget proven intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe payloads → real hook | Synthetic payloads cross the same boundary live tool calls do |
| probe runs → live session dedup state | Probe marks must never suppress (or be suppressed by) the live session's recalls |
| governance → live MEMORY.md | An automated writer touches the curated always-on router — the highest-value file in the store |
| telemetry sparsity → seat decisions | Early/thin telemetry must not produce seat changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Tampering | MEMORY.md corruption by governance | mitigate | _write_pending_block is the single surgical writer: delimiter-bounded block replace + write_atomic; byte-identical-router contract test; live run pinned by before/after checksums |
| T-03-22 | Denial of Service | live dedup pollution by probes | mitigate | Per-run temp XDG_RUNTIME_DIR in seat_probes.py (test_probe_runner.py pattern); marks cleared between seats |
| T-03-23 | Tampering | probe payload injection into shell | mitigate | Fixed argv subprocess, shell=False, payload via stdin bytes (grep-gated shell=True absence) |
| T-03-24 | Elevation of Privilege | seat demotion without proof | mitigate | D-47 structural dual gate (probe-covered AND real fires) + evidence window (≥10 sessions or ≥30 days); missing probe sidecar → zero demotions (fail-safe) |
| T-03-25 | Repudiation | seat changes invisible / unauditable | mitigate | D-48: changes exist only as the visible PENDING-SEAT-CHANGES block carrying evidence numbers; human delete = approval; no silent application path exists |
| T-03-26 | Denial of Service | seat flapping (propose/remove churn) | accept | Block replacement is idempotent and evidence-bound; churn requires telemetry actually crossing thresholds repeatedly, and the human veto bounds the blast radius to a comment block |
| T-03-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `python3 tests/memory_surface/test_phase3.py` — SeatGovernance green alongside all prior phase-3 classes
- `python3 tests/memory_surface/seat_probes.py` live — 11-seat coverage table produced
- `python3 lib/memory_surface.py seats` live — correct evidence-window refusal, MEMORY.md untouched (checksums)
- `python3 tests/memory_surface/test_routing_contract.py` + probe suite both modes — engine and recall path regression-free
- `bash tests/memory_surface/bench_recall.sh -n 20` — p95 ≤ 55 at phase exit
</verification>

<success_criteria>
- Base-floor seat membership is governed by the same telemetry as memory curation: machine-decided, probe-gated, window-gated, visible and vetoable without hand-audit (CUR-05, D-47/D-48)
- D-47's proof standard met by real runs: live per-seat probe table + live refusal + fixture end-to-end pending block, all committed in 03-SEAT-GOVERNANCE.md
- The live router is provably untouched until evidence justifies a proposal — and even then, only a pending block appears
</success_criteria>

<output>
Create `.planning/phases/03-telemetry-self-curation/03-04-SUMMARY.md` when done. Record: the live probe table summary, the refusal line, checksums, the fixture pending block, final bench numbers, and the standing note on when the first live seat decision becomes possible.
</output>
