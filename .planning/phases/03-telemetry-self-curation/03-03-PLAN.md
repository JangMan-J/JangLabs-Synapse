---
phase: 03-telemetry-self-curation
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - tests/memory_surface/run_shadow_validation.py
  - tests/memory_surface/test_phase3.py
  - settings.global.fragment.json
  - memory/_review_game.py
  - hooks/memory-review-offer.sh
autonomous: true
requirements: [CUR-04]
must_haves:
  truths:
    - "A rerunnable command runs the maintenance pass in SHADOW mode against the live store and compares its demote list against Roulette's human-keep baseline (memories with lastReviewed set in per-memory frontmatter) — emitting a machine-readable gate=OPEN|CLOSED verdict (D-45)"
    - "The comparison was actually RUN against the live store and its raw output is committed as a phase artifact — a real demonstration, not an assertion (MVR-gate discipline)"
    - "The D-39 read-rate proxy is spot-checked against real fires from live telemetry and the result is recorded before demotion thresholds are considered live"
    - "Roulette's invocation surface is removed ONLY after the gate reads OPEN: memory-review-offer.sh is deregistered from the fragment AND the live ~/.claude/settings.json, with all other hook registrations intact (D-46)"
    - "memory/_review_game.py and hooks/memory-review-offer.sh carry deprecation headers; no logic is changed and no file is deleted (physical deletion is Phase 4)"
  artifacts:
    - path: "tests/memory_surface/run_shadow_validation.py"
      provides: "D-45 shadow-vs-Roulette comparison runner emitting baseline_kept=/shadow_demoted=/kept_demoted=/gate= lines"
      contains: "gate="
    - path: ".planning/phases/03-telemetry-self-curation/03-SHADOW-VALIDATION.md"
      provides: "Committed real-run comparison artifact + D-39 proxy spot-check + explicit retirement verdict"
      contains: "Roulette retirement gate"
    - path: "memory/_review_game.py"
      provides: "Deprecation header (file kept for Phase 4 deletion)"
      contains: "DEPRECATED"
    - path: "settings.global.fragment.json"
      provides: "UserPromptSubmit block WITHOUT memory-review-offer.sh"
  key_links:
    - from: "tests/memory_surface/run_shadow_validation.py"
      to: "lib/memory_surface.py"
      via: "subprocess python3 ENGINE maintenance-shadow (read-only by construction)"
      pattern: "maintenance-shadow"
    - from: "run_shadow_validation.py"
      to: "per-memory frontmatter lastReviewed"
      via: "engine parse_frontmatter import (sys.path.insert lib — the _review_game.py pattern); NOT _tag_review.json (Pitfall G)"
      pattern: "lastReviewed"
    - from: "Task 2 (retirement)"
      to: "Task 1 gate verdict"
      via: "hard process gate — retirement executes only on gate=OPEN"
      pattern: "gate=OPEN"
---

## Phase Goal

**As a** solo operator of this box's memory harness, **I want to** have every recall fire logged, every read-confirmation detected, and curation decided automatically from that evidence, **so that** the right memories keep surfacing with zero human curation ritual — Roulette retired and router seats machine-governed.

This plan delivers the third vertical slice: after it completes, the human curation ritual is gone — Memory Roulette no longer offers rounds at prompt-submit, and the committed validation artifact proves the automated pass would not have demoted anything a human chose to keep (CUR-04).

<objective>
Validate the automated maintenance pass against Roulette's human record and retire Roulette: build the D-45 shadow-comparison runner, execute it for real against the live store, commit the comparison + D-39 proxy spot-check as a phase artifact, and — only on a gate=OPEN verdict — remove Roulette's invocation surface and mark its code deprecated (D-46).

Purpose: CUR-04 — Roulette retired as a human ritual, removed only after the automated pass is validated against it.
Output: run_shadow_validation.py, the committed 03-SHADOW-VALIDATION.md artifact, a deregistered memory-review-offer.sh, and deprecation headers.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-telemetry-self-curation/03-CONTEXT.md
@.planning/phases/03-telemetry-self-curation/03-RESEARCH.md
@.planning/phases/03-telemetry-self-curation/03-PATTERNS.md
@.planning/phases/03-telemetry-self-curation/03-02-SUMMARY.md
</context>

## Artifacts this phase produces

Full phase inventory lives in 03-01-PLAN.md. This plan creates:

- `tests/memory_surface/run_shadow_validation.py` — `--store` arg (default: live box-brain store); runs `maintenance-shadow` via subprocess; loads the human-keep baseline from per-memory frontmatter (`lastReviewed`); prints `baseline_kept=`, `shadow_demoted=`, `kept_demoted=`, `gate=OPEN|CLOSED` plus the underlying lists
- `.planning/phases/03-telemetry-self-curation/03-SHADOW-VALIDATION.md` — committed real-run artifact (D-45) including the D-39 proxy spot-check
- `settings.global.fragment.json` — memory-review-offer.sh entry removed from UserPromptSubmit
- `memory/_review_game.py`, `hooks/memory-review-offer.sh` — deprecation headers (no logic change)
- `tests/memory_surface/test_phase3.py` — `ShadowValidation` class

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-45 shadow validation — runner, real live run, committed artifact (+ D-39 proxy spot-check)</name>
  <files>tests/memory_surface/run_shadow_validation.py, tests/memory_surface/test_phase3.py</files>
  <read_first>
    - .planning/phases/03-telemetry-self-curation/03-RESEARCH.md (Q3 — the baseline: 123 memories with lastReviewed, declineCount currently 0 store-wide; Pitfall G — _tag_review.json is NOT the baseline; Q9 — the D-39 proxy validation approach and the conservatism argument)
    - memory/_review_game.py (lines ~73–77: the sys.path.insert engine-import pattern to copy; the frontmatter keys lastReviewed/declineCount/nextEligible under metadata:)
    - .planning/phases/03-telemetry-self-curation/03-02-SUMMARY.md (the live maintenance-shadow baseline JSON recorded by 03-02)
    - lib/memory_surface.py (resolve_memdir, parse_frontmatter, _memory_files — imported, not reimplemented)
  </read_first>
  <behavior>
    Contract tests (ShadowValidation class, fixture stores with synthetic telemetry):
    - Fixture where every lastReviewed memory has zero fires → kept_demoted=0, gate=OPEN (the D-43 floor protects the human-kept set)
    - Fixture where a lastReviewed memory is fired 10x/read 0x → that stem appears in kept_demoted, gate=CLOSED (the comparison provably bites — it is not a rubber stamp)
    - Runner against a fixture mutates nothing: every store file mtime unchanged after the run (shadow is read-only end to end)
    - Output is machine-parseable: exactly one line each of baseline_kept=, shadow_demoted=, kept_demoted=, gate=
  </behavior>
  <action>
Write the ShadowValidation tests first (RED), then the runner (GREEN).

`tests/memory_surface/run_shadow_validation.py` (stdlib only): import the engine via the _review_game.py sys.path.insert pattern; resolve the store from `--store` argv (default: the live box-brain store via the engine's resolve_memdir). Step 1 — run `python3 <lib>/memory_surface.py maintenance-shadow` as a subprocess (fixed argv, shell=False, MEMORY_SURFACE_DIR set to the target store) and parse its JSON. Step 2 — build the human-keep baseline by iterating `_memory_files(store)` and collecting stems whose frontmatter metadata has a non-empty `lastReviewed` (the Roulette keep-confirmation record; explicitly NOT `_tag_review.json`, which tracks tag rounds — Pitfall G). Step 3 — compute kept_demoted as the sorted intersection of the baseline set and the shadow demoted set. Print the four key=value lines plus the three lists, exit 0 always (the verdict line judges; the script reports).

Then the REAL run (the D-45 demonstration — same discipline as the Phase-2 MVR gate, no box checked by assertion): execute `python3 tests/memory_surface/run_shadow_validation.py` against the live store and paste its raw output verbatim into a new committed artifact `.planning/phases/03-telemetry-self-curation/03-SHADOW-VALIDATION.md`, together with: live telemetry record count + date span, live store memory count, the research baseline expectation (123 lastReviewed memories), and the interpretation (early telemetry → zero-fire floor protects all kept memories; the comparison's bite is proven by the fixture CLOSED test, cited by test name).

D-39 proxy spot-check (same artifact, required before demotion thresholds are considered live): extract up to 10 fire records from the live telemetry (jq over qid-records); for each, list the surfaced memory ids and whether a matching signal:"read" record followed; note anecdotally (from this session's own observable behavior where applicable) whether recall content was plausibly used without a Read — the known lower-bound divergence. Record the divergence count and the standing conservatism mitigation: demotion requires a sustained 30-day fired-never-read record at the 0.05 threshold, and maintenance-shadow provides ongoing proxy visibility. If fewer than 10 fires exist yet, spot-check all available and state the count honestly.

End the artifact with the explicit verdict line: `Roulette retirement gate: OPEN` (or CLOSED). If CLOSED — any human-kept memory in the live demote list — STOP this plan after committing the artifact: do NOT execute Task 2; record the blocker in the SUMMARY for escalation.
  </action>
  <verify>
    <automated>python3 tests/memory_surface/test_phase3.py && python3 tests/memory_surface/run_shadow_validation.py | grep -cE "^(baseline_kept|shadow_demoted|kept_demoted|gate)=" | grep -qx 4 && grep -q "Roulette retirement gate" .planning/phases/03-telemetry-self-curation/03-SHADOW-VALIDATION.md && echo D45-RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - ShadowValidation contract tests pass, including the fixture gate=CLOSED case (the comparison demonstrably bites)
    - The live run's raw output appears verbatim in the committed 03-SHADOW-VALIDATION.md, with telemetry stats and interpretation
    - The D-39 spot-check section exists with real fire records (or an honest "N fires available" note) and a divergence summary
    - kept_demoted=0 and gate=OPEN on the live run — or the plan STOPPED before Task 2 with the blocker recorded
    - Runner is read-only: fixture mtime test passes; `grep -c "shell=True" tests/memory_surface/run_shadow_validation.py` prints 0
  </acceptance_criteria>
  <done>The automated pass is validated against Roulette's human record by a real, committed, rerunnable comparison — and the retirement gate verdict is on record.</done>
</task>

<task type="auto">
  <name>Task 2: Roulette retirement — deregister offer hook, deprecation headers (D-46; GATED on Task 1 gate=OPEN)</name>
  <files>settings.global.fragment.json, memory/_review_game.py, hooks/memory-review-offer.sh</files>
  <read_first>
    - .planning/phases/03-telemetry-self-curation/03-SHADOW-VALIDATION.md (the gate verdict — this task runs ONLY on OPEN)
    - agent-harness.py (install AND remove subcommand semantics — the fragment merge only ADDS; removing a fragment entry does not deregister it live, so determine the sanctioned deregistration path: expected `./agent-harness.py remove --apply` followed by `./agent-harness.py install --apply`, symmetric with per-run backups)
    - settings.global.fragment.json (UserPromptSubmit block — memory-review-offer.sh entry between system-fingerprint.sh and lab-scope.sh)
    - .planning/phases/03-telemetry-self-curation/03-PATTERNS.md ("_review_game.py" section — the exact deprecation header text)
    - .planning/phases/03-telemetry-self-curation/03-RESEARCH.md (Q3 "Roulette invocation surface to remove" — offer is the only hook-called path; play/keep/later/toss/flip/tag-* are manual CLI, not gated)
  </read_first>
  <action>
HARD GATE: read the artifact's verdict line first. If it is not `Roulette retirement gate: OPEN`, stop and record the blocker — CUR-04's wording ("removed only after validated") is the requirement, not a preference.

Fragment edit: delete the memory-review-offer.sh hook entry from the UserPromptSubmit block of settings.global.fragment.json (leave system-fingerprint.sh and lab-scope.sh untouched). Validate JSON with jq.

Live deregistration: the install merge is add-only, so a removed fragment entry stays live until removed. Before acting, capture the full live hook registration inventory: `jq '[.hooks[][].hooks[].command] | sort' ~/.claude/settings.json > /tmp/hooks-before.json`. Then run the sanctioned symmetric cycle per what agent-harness.py actually implements (read it first): expected `./agent-harness.py remove --apply && ./agent-harness.py install --apply` — remove deregisters harness-managed entries, install re-adds the current fragment (now without the offer hook); per-run backups land under .uninstall-backups/ and .install-backups/. If reading agent-harness.py shows remove does NOT cover this case, fall back to a direct targeted jq edit of ~/.claude/settings.json deleting only the memory-review-offer.sh entry (write to temp + validate + replace) — never touching permissions keys (standing guard). After either path: `jq '[.hooks[][].hooks[].command] | sort' ~/.claude/settings.json > /tmp/hooks-after.json` and diff — the ONLY change must be the memory-review-offer.sh removal.

Deprecation headers (no logic changes): memory/_review_game.py gets the PATTERNS header text after the shebang/docstring, before imports — DEPRECATED (Phase 3, date), Roulette retired, replaced by the automated maintenance pass, hook registration removed, physical deletion deferred to Phase 4. hooks/memory-review-offer.sh gets a comment line under its shebang: deregistered in Phase 3 (CUR-04/D-46), file retained for Phase 4 deletion. Neither file is deleted; no function body changes.
  </action>
  <verify>
    <automated>jq -e '[.hooks.UserPromptSubmit[].hooks[]?.command] | map(select(test("memory-review-offer"))) | length == 0' ~/.claude/settings.json && jq -e '[.hooks.UserPromptSubmit[].hooks[]?.command] | map(select(test("memory-review-offer"))) | length == 0' settings.global.fragment.json && grep -q "DEPRECATED" memory/_review_game.py && grep -q "Phase 3" hooks/memory-review-offer.sh && python3 tests/memory_surface/test_review_game.py</automated>
  </verify>
  <acceptance_criteria>
    - memory-review-offer.sh absent from both the fragment and the live ~/.claude/settings.json UserPromptSubmit blocks
    - The before/after live hook inventory diff shows exactly one removal and zero other changes (recorded in the SUMMARY)
    - system-fingerprint.sh and lab-scope.sh registrations intact; per-run backups exist for the cycle
    - Both deprecation headers grep-verifiable; `python3 tests/memory_surface/test_review_game.py` still green (header-only change)
    - `jq . settings.global.fragment.json` exits 0
  </acceptance_criteria>
  <done>Roulette no longer fires at prompt-submit on this box, its code is marked deprecated but intact for Phase 4's sweep, and every other hook registration is untouched — executed only behind the OPEN validation gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shadow runner → live store | The validation reads the live store; any write would contaminate the baseline it is validating against |
| retirement → live settings.json | Deregistering one hook must not disturb the rest of the live hook surface (or any permissions key) |
| validation verdict → retirement action | An unvalidated retirement would violate CUR-04's ordering requirement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-16 | Repudiation | validation honesty | mitigate | MVR discipline: raw runner output committed verbatim in 03-SHADOW-VALIDATION.md; rerunnable command; fixture CLOSED test proves the comparison can fail |
| T-03-17 | Tampering | shadow runner mutating the store | mitigate | Runner only shells out to maintenance-shadow (no-write by 03-02 contract) and reads frontmatter; fixture mtime test pins read-only behavior |
| T-03-18 | Tampering | live settings.json collateral damage | mitigate | Before/after jq inventory diff must show exactly one removal; symmetric remove+install with per-run backups; permissions keys never touched (config-drift-guard enforces from the runtime side) |
| T-03-19 | Elevation of Privilege | retirement without validation | mitigate | Hard process gate: Task 2 reads the committed verdict line and refuses on anything but OPEN |
| T-03-20 | Denial of Service | wrong baseline source (_tag_review.json) | mitigate | Pitfall G pinned: baseline is per-memory frontmatter lastReviewed; read_first names the distinction; contract test fixtures build per-memory baselines |
| T-03-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `python3 tests/memory_surface/test_phase3.py` — ShadowValidation green alongside prior classes
- `python3 tests/memory_surface/run_shadow_validation.py` — four key=value lines, gate verdict
- `jq -e` absence checks on fragment + live settings; before/after inventory diff clean
- `python3 tests/memory_surface/test_review_game.py` — game untouched functionally
- 03-SHADOW-VALIDATION.md committed with verdict + D-39 spot-check
</verification>

<success_criteria>
- The automated maintenance pass is validated against Roulette's human keep-record by a real committed comparison run — no human-kept memory in the demote list (CUR-04 precondition, D-45)
- The D-39 proxy concern is examined with real telemetry and recorded before thresholds are live
- Roulette's invocation surface is gone from the live box, behind the gate, with deprecation markers in place and physical deletion left to Phase 4 (D-46)
</success_criteria>

<output>
Create `.planning/phases/03-telemetry-self-curation/03-03-SUMMARY.md` when done. Record: the gate verdict, the live-settings before/after diff, the backup paths from the remove/install cycle, and — if the gate was CLOSED — the exact kept_demoted list as the escalation blocker.
</output>
